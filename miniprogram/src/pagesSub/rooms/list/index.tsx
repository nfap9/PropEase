import { View, Text } from '@tarojs/components';
import './index.scss';

export default function RoomsListPage() {
  return (
    <View className="rooms-list-page">
      <Text>房间列表</Text>
    </View>
  );
}
