import { View, Text } from '@tarojs/components';
import './index.scss';

export default function LeasesListPage() {
  return (
    <View className="leases-list-page">
      <Text>租约列表</Text>
    </View>
  );
}
