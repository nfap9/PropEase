# Development: install deps and run pnpm dev (source mounted in compose)
FROM node:20-alpine

WORKDIR /app

RUN corepack enable pnpm

COPY package.json pnpm-lock.yaml* ./
RUN pnpm install --frozen-lockfile

COPY . .
RUN pnpm exec prisma generate

EXPOSE 8000

ENV PORT=8000
CMD ["pnpm", "dev"]
