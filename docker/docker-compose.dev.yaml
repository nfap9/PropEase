# Development docker-compose configuration（后端为 api）
# Usage: cd docker && docker compose -f docker-compose.dev.yaml up
# ==================================================================

x-shared-env: &shared-api-env
  DATABASE_URL: postgresql://postgres:postgres@postgres:5432/apartment_ultra
  SECRET_KEY: ${SECRET_KEY:-your-super-secret-key-change-in-production}
  # 开发环境使用宽松 CORS，允许所有来源
  CORS_ORIGINS: "*"
  DEBUG: "true"

services:
  postgres:
    image: postgres:15-alpine
    container_name: apartment_ultra_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: apartment_ultra
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: apartment_ultra_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  api:
    build:
      context: ..
      dockerfile: api/Dockerfile.dev
    container_name: apartment_ultra_api
    environment:
      <<: *shared-api-env
      PORT: "8000"
    ports:
      - "8000:8000"
    volumes:
      - ../api:/app/api
      # 保留容器内的 node_modules，避免被宿主机覆盖
      - /app/api/node_modules
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
    command: sh -c "pnpm --filter apartment-ultra-api exec prisma generate && pnpm --filter apartment-ultra-api dev"

  web:
    build:
      context: ..
      dockerfile: web/Dockerfile
    container_name: apartment_ultra_web
    environment:
      # 客户端请求：浏览器访问宿主机的 localhost:8000
      NEXT_PUBLIC_API_URL: http://localhost:8000/api/v1
      # 服务端请求：Docker 内部网络使用服务名
      API_URL_SERVER: http://api:8000/api/v1
    ports:
      - "3000:3000"
    volumes:
      - ../web:/app/web
      # 保留容器内的 node_modules，避免被宿主机覆盖
      - /app/web/node_modules
      - /app/node_modules
    depends_on:
      - api

volumes:
  postgres_data:
  redis_data:
