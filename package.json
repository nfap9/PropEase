{
  "name": "apartment-ultra",
  "version": "0.1.0",
  "private": true,
  "description": "Apartment Ultra - 多租户公寓管理系统",
  "scripts": {
    "dev:api": "PORT=8000 pnpm --filter apartment-ultra-api dev",
    "dev:web": "pnpm --filter frontend dev",
    "build": "pnpm -r run build",
    "lint": "pnpm --filter apartment-ultra-api run lint && pnpm --filter frontend run lint:fix",
    "format": "prettier --write \"api/src/**/*.ts\" \"web/src/**/*.{ts,tsx}\"",
    "format:check": "prettier --check \"api/src/**/*.ts\" \"web/src/**/*.{ts,tsx}\"",
    "type-check": "pnpm --filter apartment-ultra-api run type-check && pnpm --filter frontend run type-check",
    "test": "pnpm --filter apartment-ultra-api run test && pnpm --filter frontend run test:run"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2"
  },
  "engines": {
    "node": ">=18"
  }
}
