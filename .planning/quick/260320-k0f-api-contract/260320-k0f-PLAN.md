---
phase: quick
plan: "260320-k0f"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "Audit report documents which modules follow api-contract"
    - "Identified gaps: modules/types not using api-contract"
  artifacts:
    - path: ".planning/quick/260320-k0f-api-contract/audit.md"
      provides: "api-contract adherence audit results"
  key_links: []
---

<objective>
检查 api、tenant-web、admin-web 三个模块的 API 类型定义是否遵循 @apartment-ultra/api-contract，找出未使用契约的遗漏点。
</objective>

<execution_context>
@/Users/shen/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@packages/api-contract/src/index.ts
@packages/web-api-client/src/index.ts
@tenant-web/src/types/index.ts
@admin-web/src/types/index.ts
@api/src/middlewares/responseWrapper.ts
@api/src/errors/base.ts
</context>

<tasks>

<task type="auto">
  <name>Audit api-contract adherence across all modules</name>
  <files>.planning/quick/260320-k0f-api-contract/audit.md</files>
  <action>
生成审计报告，检查以下维度：

1. **api 模块**：是否在路由/控制器返回值中使用 api-contract 的 SuccessBody 类型；错误码是否使用 BusinessCode；prisma model 类型是否与 api-contract 类型定义一致。

   检查文件：
   - api/src/middlewares/responseWrapper.ts（已确认使用 BusinessCode 和 SuccessBody）
   - api/src/errors/base.ts（已确认使用 BusinessCode）
   - api/src/routes/v1/apartments.ts（检查 Apartment/ApartmentCreate 等返回类型）
   - api/src/routes/v1/bills.ts（检查 Bill 相关类型）
   - api/src/routes/v1/organizations.ts（检查 Organization 相关类型）
   - api/src/routes/v1/subscriptions.ts（检查订阅相关类型）

2. **tenant-web 模块**：API client 文件是否从 @/types（等于 api-contract）导入类型；是否存在本地定义的类型与 api-contract 重复。

   检查文件（使用 Glob 匹配）：
   - tenant-web/src/lib/api/*.ts（所有 API client 文件）
   - tenant-web/src/types/index.ts（已确认 re-export api-contract）

3. **admin-web 模块**：同上。

   检查文件：
   - admin-web/src/lib/api/*.ts

4. **packages/web-api-client**：已确认使用 BusinessCode、SuccessBody、ErrorResponseBody 等。

**输出格式**（写入 audit.md）：

```
## api-contract adherence audit

### api 模块
- [ok/gap] 位置：描述
- 具体发现...

### tenant-web 模块
- [ok/gap] 位置：描述
- 具体发现...

### admin-web 模块
- [ok/gap] 位置：描述
- 具体发现...

### 已知未遵循 api-contract 的类型/位置

| 模块 | 文件 | 类型/变量 | api-contract 对应 | 严重程度 |
|------|------|-----------|------------------|---------|
| ...  | ...  | ...       | ...              | ...     |

### 结论
- 已正确遵循 api-contract 的主要模块：...
- 主要缺口：...
```
  </action>
  <verify>File .planning/quick/260320-k0f-api-contract/audit.md created and contains audit results for all three modules</verify>
  <done>audit.md 已生成，包含三个模块的完整审计结果</done>
</task>

</tasks>

<verification>
grep -c "api-contract" .planning/quick/260320-k0f-api-contract/audit.md
</verification>

<success_criteria>
- audit.md 包含 api、tenant-web、admin-web 三个模块的审计结果
- 标注每个模块的 ok/gap 状态
- 列出未遵循 api-contract 的具体位置和类型
</success_criteria>

<output>
After completion, create `.planning/quick/260320-k0f-api-contract/260320-k0f-SUMMARY.md`
</output>
