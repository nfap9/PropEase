---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tenant-web/package.json
  - admin-web/package.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "tenant-web runs on port 3000"
    - "admin-web runs on port 3001"
    - "Ports are explicitly configured in scripts"
  artifacts:
    - path: "tenant-web/package.json"
      contains: "PORT=3000"
    - path: "admin-web/package.json"
      contains: "-p 3001"
---

<objective>
Fix the admin and tenant web port conflict by explicitly configuring both to use different ports.

Purpose: Ensure tenant-web and admin-web can run simultaneously without port conflicts.
Output: Explicit port configuration in both package.json files.
</objective>

<execution_context>
@/Users/shen/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Current configuration:
- tenant-web: `"dev": "next dev"` (defaults to 3000, but not explicit)
- admin-web: `"dev": "next dev -p 3001"` (explicitly 3001)

Both already use different ports, but tenant-web lacks explicit PORT configuration.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit PORT=3000 to tenant-web dev script</name>
  <files>tenant-web/package.json</files>
  <action>Change tenant-web dev script from `"dev": "next dev"` to `"dev": "PORT=3000 next dev"`. This makes the port explicit and matches the documented default port for tenant-web.</action>
  <verify>
    <automated>grep '"dev": "PORT=3000 next dev"' tenant-web/package.json</automated>
  </verify>
  <done>tenant-web dev script explicitly uses PORT=3000</done>
</task>

<task type="auto">
  <name>Task 2: Verify admin-web port 3001 configuration</name>
  <files>admin-web/package.json</files>
  <action>Confirm admin-web already has `"dev": "next dev -p 3001"` and ensure the start script also uses 3001 (`"start": "next start -p 3001"`). Verify build script remains unchanged.</action>
  <verify>
    <automated>grep '"dev": "next dev -p 3001"' admin-web/package.json && grep '"start": "next start -p 3001"' admin-web/package.json</automated>
  </verify>
  <done>admin-web uses port 3001 for dev and start scripts</done>
</task>

</tasks>

<verification>
Verify both package.json files have explicit, non-conflicting port configurations.
</verification>

<success_criteria>
- tenant-web runs on port 3000
- admin-web runs on port 3001
- Both can be started simultaneously without port conflicts
</success_criteria>

<output>
After completion, create `.planning/quick/260319-m9l-admin-tenant/260319-m9l-SUMMARY.md`
</output>
