---
phase: 02-修复链接指向
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tenant-web/src/app/settings/page.tsx
autonomous: true
requirements:
  - ROUTE-04
  - ROUTE-06

must_haves:
  truths:
    - "组织管理卡片已从首页移除"
    - "团队设置和权限管理合并为单一入口"
    - "订阅管理图标已更新为ShoppingBag"
  artifacts:
    - path: "tenant-web/src/app/settings/page.tsx"
      provides: "SETTINGS_ITEMS配置"
      min_lines: 60
  key_links:
    - from: "SETTINGS_ITEMS"
      to: "/settings/team"
      via: "团队与权限卡片href"
      pattern: "href: '/settings/team'"
---

<objective>
更新设置首页的SETTINGS_ITEMS配置：移除"组织管理"卡片，合并"团队设置"和"权限管理"为"团队与权限"，将"订阅管理"图标从CreditCard改为ShoppingBag。
</objective>

<context>
@tenant-web/src/app/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新SETTINGS_ITEMS配置</name>
  <files>tenant-web/src/app/settings/page.tsx</files>
  <read_first>
    - tenant-web/src/app/settings/page.tsx
  </read_first>
  <action>
修改 tenant-web/src/app/settings/page.tsx 中的 SETTINGS_ITEMS 数组和导入：

**1. 修改导入 (第6-13行):**
- 从 lucide-react 导入中移除 `Building2` 和 `CreditCard`
- 添加 `ShoppingBag` (如果不存在)

**2. 修改 SETTINGS_ITEMS 数组 (第26-67行):**

最终结果应该是4个卡片 (不是5个):

```javascript
const SETTINGS_ITEMS = [
  {
    title: '团队与权限',
    description: '管理团队成员和角色权限',
    href: '/settings/team',
    icon: Users,
    testId: SETTINGS.TEAM_LINK,
    cardTestId: SETTINGS.TEAM_CARD,
  },
  {
    title: '消息触达',
    description: '管理租客短信模板与发送记录',
    href: '/settings/notifications',
    icon: MessageSquareMore,
    testId: 'settings-notifications-link',
    cardTestId: 'settings-notifications-card',
  },
  {
    title: '订阅管理',
    description: '管理订阅服务和账单',
    href: '/settings/subscription',
    icon: ShoppingBag,  // 从 CreditCard 改为 ShoppingBag
    testId: 'settings-subscription-link',
    cardTestId: SETTINGS.SUBSCRIPTION_CARD,
  },
];
```

**3. 更新 SETTINGS 常量 (第16-24行):**
移除 ORG_CARD 相关常量，因为组织管理卡片已删除。

**关键变更:**
- 删除: Building2 导入, CreditCard 导入
- 添加: ShoppingBag 导入 (已在 lucide-react 中)
- 删除整个"组织管理"卡片对象 (第59-66行)
- 将"团队设置"改为"团队与权限"，描述改为"管理团队成员和角色权限"
- 删除"权限管理"卡片对象
- 将"订阅管理"的 icon 从 CreditCard 改为 ShoppingBag
</action>
  <verify>
    <automated>
      grep -c "组织管理" tenant-web/src/app/settings/page.tsx || echo "0"
      grep "ShoppingBag" tenant-web/src/app/settings/page.tsx | head -3
      grep "团队与权限" tenant-web/src/app/settings/page.tsx
    </automated>
  </verify>
  <done>
- "组织管理" 卡片已删除
- "团队与权限" 卡片存在，描述为"管理团队成员和角色权限"
- "订阅管理" 使用 ShoppingBag 图标
- SETTINGS_ITEMS 数组长度为3项
</done>
</task>

</tasks>

<verification>
- grep "组织管理" tenant-web/src/app/settings/page.tsx 应该无输出
- grep "ShoppingBag" tenant-web/src/app/settings/page.tsx 应该有输出
- grep "团队与权限" tenant-web/src/app/settings/page.tsx 应该有输出
</verification>

<success_criteria>
1. 组织管理卡片不再出现在设置首页
2. 团队与权限作为唯一入口指向 /settings/team
3. 订阅管理卡片图标为 ShoppingBag
4. 消息触达卡片保持不变
</success_criteria>

<output>
After completion, create `.planning/phases/02-修复链接指向/02-01-SUMMARY.md`
</output>
