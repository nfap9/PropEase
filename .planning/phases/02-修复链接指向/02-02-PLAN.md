---
phase: 02-修复链接指向
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tenant-web/src/app/settings/subscription/page.tsx
autonomous: true
requirements:
  - ROUTE-06

must_haves:
  truths:
    - "订阅管理页面标题图标已更新为ShoppingBag"
  artifacts:
    - path: "tenant-web/src/app/settings/subscription/page.tsx"
      provides: "订阅管理页面标题"
      min_lines: 75
  key_links:
    - from: "subscription/page.tsx"
      to: "lucide-react"
      via: "ShoppingBag import"
      pattern: "ShoppingBag"
---

<objective>
将订阅管理页面的标题图标从 CreditCard 改为 ShoppingBag，以匹配设置首页卡片的图标。
</objective>

<context>
@tenant-web/src/app/settings/subscription/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新订阅管理页面标题图标</name>
  <files>tenant-web/src/app/settings/subscription/page.tsx</files>
  <read_first>
    - tenant-web/src/app/settings/subscription/page.tsx
  </read_first>
  <action>
修改 tenant-web/src/app/settings/subscription/page.tsx:

**1. 修改导入 (第11-20行):**
- 从 lucide-react 导入中将 `CreditCard` 替换为 `ShoppingBag`

**2. 修改页面标题图标 (第72-74行):**
将:
```javascript
<h1 className="flex items-center gap-2 text-3xl font-bold" data-testid={SUBSCRIPTION.HEADING}>
  <CreditCard className="h-8 w-8" />
  我的订阅
</h1>
```
改为:
```javascript
<h1 className="flex items-center gap-2 text-3xl font-bold" data-testid={SUBSCRIPTION.HEADING}>
  <ShoppingBag className="h-8 w-8" />
  我的订阅
</h1>
```
</action>
  <verify>
    <automated>
      grep "ShoppingBag" tenant-web/src/app/settings/subscription/page.tsx | head -2
      grep "CreditCard" tenant-web/src/app/settings/subscription/page.tsx || echo "未找到CreditCard"
    </automated>
  </verify>
  <done>
- ShoppingBag 已导入
- 页面标题使用 ShoppingBag 图标
- CreditCard 不再出现在文件中
</done>
</task>

</tasks>

<verification>
- grep "ShoppingBag" tenant-web/src/app/settings/subscription/page.tsx 应该有输出 (icon定义)
- grep "CreditCard" tenant-web/src/app/settings/subscription/page.tsx 应该无输出
</verification>

<success_criteria>
1. 订阅管理页面标题图标与首页卡片图标一致 (均为 ShoppingBag)
2. CreditCard 不再出现在文件中
</success_criteria>

<output>
After completion, create `.planning/phases/02-修复链接指向/02-02-SUMMARY.md`
</output>
