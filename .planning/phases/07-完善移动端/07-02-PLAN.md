---
phase: 07-完善移动端
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - mobile/tamagui.config.ts
  - mobile/constants/Colors.ts
  - mobile/components/ui/Card.tsx
  - mobile/components/ui/Button.tsx
  - mobile/components/ui/ListItem.tsx
  - mobile/components/ui/Header.tsx
  - mobile/components/index.ts
  - mobile/app/(tabs)/utilities.tsx
autonomous: true
requirements:
  - MOBILE-01
  - MOBILE-04
---

<objective>
建立Tamagui共享基础组件，并拆分utilities.tsx大文件。

Purpose: 建立可复用的Tamagui基础组件库，为后续UI重构提供统一组件；将1193行的utilities.tsx按业务功能拆分为独立组件
Output: mobile/components/ui/ 下的基础组件 + 拆分后的utilities.tsx
</objective>

<execution_context>
@/Users/shen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@mobile/tamagui.config.ts
@mobile/constants/Colors.ts
@mobile/app/(tabs)/utilities.tsx
@.planning/phases/07-完善移动端/07-01-SUMMARY.md (if exists)

<interfaces>
<!-- Tamagui config provides these design tokens -->
From mobile/tamagui.config.ts:
```typescript
// Color tokens available:
primary500: '#3B82F6', primary600: '#2563EB', success600: '#10B981',
warning600: '#F97316', danger600: '#EF4444', gray500: '#64748B',
textPrimary: '#1F2937', textSecondary: '#6B7280', textMuted: '#9CA3AF',
background: '#F8FAFC', surface: '#FFFFFF'

// Size tokens: 0, 0.25, 0.5, 0.75, 1(=12), 1.5(=16), 2(=20), ...
// Radius tokens: 0, 1(=4), 2(=8), 3(=12), 4(=16), ...
```

Current Colors.ts has limited colors (tintColorLight, tintColorDark) - needs alignment with tamagui.config.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 同步 Colors.ts 与 Tamagui 设计令牌</name>
  <files>mobile/constants/Colors.ts</files>
  <read_first>
    - mobile/constants/Colors.ts
    - mobile/tamagui.config.ts
  </read_first>
  <action>
    更新 mobile/constants/Colors.ts 使其与 tamagui.config.ts 中的设计令牌保持一致：

    当前 Colors.ts 只有 tintColorLight/dark，需要扩展为完整的颜色系统：

    ```typescript
    const tintColorLight = '#2f95dc';

    export const Colors = {
      // 主色（对应 tamagui primary600）
      primary: '#2563EB',
      primaryLight: '#DBEAFE',

      // 成功色（对应 tamagui success600）
      success: '#10B981',
      successLight: '#D1FAE5',

      // 警告色（对应 tamagui warning600）
      warning: '#F97316',
      warningLight: '#FFEDD5',

      // 危险色（对应 tamagui danger600）
      danger: '#EF4444',
      dangerLight: '#FEE2E2',

      // 灰色梯度
      gray: {
        50: '#F8FAFC',
        100: '#F1F5F9',
        200: '#E2E8F0',
        300: '#CBD5E1',
        400: '#94A3B8',
        500: '#64748B',
        600: '#475569',
        700: '#334155',
        800: '#1E293B',
        900: '#0F172A',
      },

      // 文字颜色
      textPrimary: '#1F2937',
      textSecondary: '#6B7280',
      textMuted: '#9CA3AF',

      // 背景色
      background: '#F8FAFC',
      surface: '#FFFFFF',

      // 边框色
      border: '#E5E7EB',
      borderLight: '#F3F4F6',

      // 兼容
      tint: tintColorLight,
      light: {
        text: '#000',
        background: '#fff',
        tint: tintColorLight,
        tabIconDefault: '#ccc',
        tabIconSelected: tintColorLight,
      },
    }
    ```

    注意：保持向后兼容，不删除现有属性。
  </action>
  <acceptance_criteria>
    - [ ] Colors 对象包含所有 tamagui.config.ts 中的关键颜色
    - [ ] 现有代码中 Colors.primary, Colors.success 等引用仍然有效
    - [ ] `pnpm type-check:mobile` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/shen/Desktop/workspace/apartment-ultra && pnpm type-check:mobile 2>&1 | head -30</automated>
  </verify>
  <done>Colors.ts 与 Tamagui 设计令牌对齐，包含所有必要颜色</done>
</task>

<task type="auto">
  <name>Task 2: 创建 Tamagui 共享基础组件</name>
  <files>
    - mobile/components/ui/Card.tsx
    - mobile/components/ui/Button.tsx
    - mobile/components/ui/ListItem.tsx
    - mobile/components/ui/Header.tsx
    - mobile/components/index.ts
  </files>
  <read_first>
    - mobile/tamagui.config.ts
    - mobile/app/(tabs)/bills.tsx (查看卡片样式模式)
    - mobile/app/(tabs)/customers.tsx (查看列表项样式模式)
  </read_first>
  <action>
    创建 mobile/components/ui/ 目录下的共享基础组件（使用 Tamagui）：

    1. **mobile/components/ui/Card.tsx** - 卡片组件：
    ```typescript
    import { YStack, styled } from 'tamagui'

    export const Card = styled(YStack, {
      backgroundColor: '$surface',
      borderRadius: '$4',
      padding: '$3',
      borderWidth: 1,
      borderColor: '$borderLight',
      shadowColor: '#000',
      shadowOffset: { width: 0, height: 2 },
      shadowOpacity: 0.05,
      shadowRadius: 4,
      elevation: 2,
    })
    ```

    2. **mobile/components/ui/Button.tsx** - 按钮组件（变体：primary, secondary, danger）：
    ```typescript
    import { Button as TamaguiButton } from 'tamagui'
    import { styled } from 'tamagui'

    export const PrimaryButton = styled(TamaguiButton, {
      backgroundColor: '$primary600',
      color: 'white',
      borderRadius: '$3',
      paddingHorizontal: '$3',
      paddingVertical: '$2',
      fontWeight: '700',
    })

    export const SecondaryButton = styled(TamaguiButton, {
      backgroundColor: 'transparent',
      color: '$primary600',
      borderRadius: '$3',
      borderWidth: 1,
      borderColor: '$primary600',
      paddingHorizontal: '$3',
      paddingVertical: '$2',
    })
    ```

    3. **mobile/components/ui/ListItem.tsx** - 列表项组件：
    ```typescript
    import { XStack, YStack, Text, styled } from 'tamagui'

    export const ListItem = styled(XStack, {
      backgroundColor: '$surface',
      borderRadius: '$4',
      padding: '$3',
      borderWidth: 1,
      borderColor: '$borderLight',
      alignItems: 'center',
    })

    export const ListItemText = styled(Text, {
      color: '$textPrimary',
      fontSize: 14,
      fontWeight: '600',
    })
    ```

    4. **mobile/components/ui/Header.tsx** - 页面头部组件（带渐变背景）：
    ```typescript
    import { YStack, Text, XStack, styled } from 'tamagui'

    export const PageHeader = styled(YStack, {
      backgroundColor: '$primary600',
      paddingTop: 24,
      paddingBottom: 32,
      paddingHorizontal: 20,
      borderBottomLeftRadius: 40,
      borderBottomRightRadius: 40,
    })

    export const PageTitle = styled(Text, {
      color: 'white',
      fontSize: 24,
      fontWeight: '900',
      marginBottom: 16,
      letterSpacing: -0.5,
    })
    ```

    5. **mobile/components/index.ts** - 统一导出：
    ```typescript
    export { Card } from './ui/Card'
    export { PrimaryButton, SecondaryButton } from './ui/Button'
    export { ListItem, ListItemText } from './ui/ListItem'
    export { PageHeader, PageTitle } from './ui/Header'
    ```
  </action>
  <acceptance_criteria>
    - [ ] Card, Button, ListItem, Header 组件创建完成
    - [ ] mobile/components/index.ts 统一导出所有组件
    - [ ] `pnpm type-check:mobile` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/shen/Desktop/workspace/apartment-ultra && pnpm type-check:mobile 2>&1 | head -30</automated>
  </verify>
  <done>Tamagui共享基础组件（Card, Button, ListItem, Header）创建完成</done>
</task>

<task type="auto">
  <name>Task 3: 拆分 utilities.tsx 大文件</name>
  <files>
    - mobile/app/(tabs)/utilities.tsx
    - mobile/components/utilities/ReadingInput.tsx
    - mobile/components/utilities/QueueCard.tsx
    - mobile/components/utilities/index.ts
  </files>
  <read_first>
    - mobile/app/(tabs)/utilities.tsx (完整文件，理解其结构)
    - mobile/components/index.ts
  </read_first>
  <action>
    将 1193 行的 utilities.tsx 按业务功能拆分为独立组件：

    1. 首先阅读 utilities.tsx 理解其完整结构，识别主要功能区块：
       - 筛选器（FilterType）
       - 读数录入表单（ReadingInput）
       - 房间队列卡片（QueueCard）
       - 队列状态管理

    2. 创建 mobile/components/utilities/ 目录

    3. 创建子组件：
       - **ReadingInput.tsx**: 水电读数录入表单
       - **QueueCard.tsx**: 单个房间队列项（包含状态显示、录入按钮）
       - **FilterTabs.tsx**: 状态筛选 Tab

    4. 重构 utilities.tsx：
       - 导入新组件
       - 保留核心逻辑（useQuery数据获取、mutation提交）
       - 使用导入的子组件替代内联实现

    5. 创建 mobile/components/utilities/index.ts 统一导出

    注意：
    - 保持原有功能不变
    - 确保 TypeScript 类型完整
    - 组件使用 Colors 常量保持一致
  </action>
  <acceptance_criteria>
    - [ ] utilities.tsx 从 1193 行拆分为约 400-500 行主文件 + 子组件
    - [ ] 子组件位于 mobile/components/utilities/ 目录
    - [ ] `pnpm type-check:mobile` 通过
    - [ ] utilities 页面功能保持一致
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/shen/Desktop/workspace/apartment-ultra && pnpm type-check:mobile 2>&1 | head -30</automated>
  </verify>
  <done>utilities.tsx 拆分为多个子组件，总行数减少约50%</done>
</task>

</tasks>

<verification>
整体验证：
1. `pnpm type-check:mobile` 完全通过
2. `ls mobile/components/ui/` 确认组件文件存在
3. `wc -l mobile/app/(tabs)/utilities.tsx` 确认行数减少
4. `grep -l "from.*tamagui" mobile/components/ui/*.tsx` 确认使用 Tamagui
</verification>

<success_criteria>
- Colors.ts 与 Tamagui 设计令牌同步
- Tamagui 基础组件（Card, Button, ListItem, Header）创建完成
- utilities.tsx 拆分完成，保留全部功能
- 所有代码通过 TypeScript 类型检查
</success_criteria>

<output>
After completion, create `.planning/phases/07-完善移动端/07-02-SUMMARY.md`
</output>
