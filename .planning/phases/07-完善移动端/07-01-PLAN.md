---
phase: 07-完善移动端
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/services/api/client.ts
  - mobile/app/(tabs)/customers.tsx
  - mobile/app/settings/reports.tsx
  - mobile/services/api/index.ts
autonomous: true
requirements:
  - MOBILE-01
  - MOBILE-02
  - MOBILE-03

must_haves:
  truths:
    - "Token refresh uses singleton lock preventing concurrent refresh race"
    - "customers.tsx uses proper types instead of any"
    - "reports.tsx uses DashboardOverview type instead of any"
    - "All mobile code passes type-check with no any types"
  artifacts:
    - path: "mobile/services/api/client.ts"
      provides: "Token refresh with singleton lock mechanism"
      contains: "refreshLock"
    - path: "mobile/app/(tabs)/customers.tsx"
      provides: "Tenant list with proper typing"
      exports: "TenantWithLease"
    - path: "mobile/app/settings/reports.tsx"
      provides: "Reports page with DashboardOverview typing"
      exports: "DashboardOverview"
  key_links:
    - from: "mobile/services/api/client.ts"
      to: "mobile/services/api/reports.ts"
      via: "import DashboardOverview"
    - from: "mobile/services/api/client.ts"
      to: "mobile/services/api/tenants.ts"
      via: "import Tenant, TenantCreate, TenantUpdate"
---

<objective>
修复Token竞态条件和类型安全问题。

Purpose: 解决移动端两个高优先级质量问题：1) API client的401刷新竞态 2) TypeScript类型不安全（any）
Output: 修复后的 client.ts、customers.tsx、reports.tsx
</objective>

<execution_context>
@/Users/shen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/MOBILE-ANALYSIS.md
@mobile/services/api/client.ts
@mobile/services/api/reports.ts
@mobile/services/api/tenants.ts
@mobile/services/api/leases.ts
@packages/api-contract/src/tenants.ts
@packages/api-contract/src/reports.ts
@packages/api-contract/src/leases.ts

<interfaces>
<!-- Key types from api-contract that must be used correctly -->

From @apartment-ultra/api-contract:
```typescript
// packages/api-contract/src/tenants.ts
interface Tenant {
  id: string;
  organization_id: string;
  name: string;
  phone: string | null;
  // NOTE: base Tenant does NOT have lease property
}

// packages/api-contract/src/leases.ts
interface Lease {
  id: string;
  room_id: string;
  tenant_id: string;
  start_date: string;
  end_date: string | null;
  billing_day: number;
  monthly_rent: number;
  deposit: number;
  water_rate: number;
  electricity_rate: number;
  is_active: boolean;
  notes: string | null;
  room?: Room;
  tenant?: Tenant;
  created_at: string;
}

// packages/api-contract/src/reports.ts
interface DashboardOverview {
  total_apartments: number;
  total_rooms: number;
  occupied_rooms: number;
  available_rooms: number;
  total_tenants: number;
  active_leases: number;
  occupancy_rate: number;
  monthly_revenue: number;
  pending_bills: number;
  overdue_bills: number;
  rooms_missing_initial_readings?: number;
}
```

Current client.ts race condition (lines 207-224):
```typescript
// 401时刷新token后重试，但使用了闭包中的旧headers
if (response.status === 401 && !skipAuth) {
  const refreshed = await handleUnauthorized()
  if (refreshed) {
    // requestHeaders 在闭包中是旧值
    const newToken = await getToken()
    if (newToken) {
      requestHeaders['Authorization'] = `Bearer ${newToken}`
    }
    response = await fetch(`${API_URL}${url}`, { ...fetchOptions, headers: requestHeaders })
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 client.ts Token刷新竞态</name>
  <files>mobile/services/api/client.ts</files>
  <read_first>
    - mobile/services/api/client.ts (full file, focus on handleUnauthorized and request functions)
  </read_first>
  <action>
    在 client.ts 中实现单例锁机制修复Token刷新竞态：

    1. 在文件顶部添加 refreshLock 变量（模块级别单例）：
    ```typescript
    let refreshLock: Promise<boolean> | null = null
    ```

    2. 修改 handleUnauthorized 函数使用锁机制：
    ```typescript
    async function handleUnauthorized(): Promise<boolean> {
      // 如果已有刷新在进行，等待它完成
      if (refreshLock) {
        return refreshLock
      }

      // 创建新的刷新Promise
      refreshLock = (async () => {
        try {
          const refreshToken = await secureStorage.getItem('refresh_token')
          if (!refreshToken) {
            return false
          }
          const response = await fetch(`${API_URL}/auth/refresh`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ refresh_token: refreshToken }),
          })
          if (!response.ok) {
            return false
          }
          const responseData = await response.json()
          const tokenData = responseData && 'code' in responseData && responseData.code === 0
            ? responseData.data
            : responseData
          const { access_token, refresh_token } = tokenData
          await secureStorage.setItem('access_token', access_token)
          await secureStorage.setItem('refresh_token', refresh_token)
          return true
        } catch {
          return false
        } finally {
          refreshLock = null
        }
      })()

      return refreshLock
    }
    ```

    3. 修改 request 函数中的401处理逻辑，确保使用最新的token（不要在闭包中捕获旧headers）：
    ```typescript
    if (response.status === 401 && !skipAuth) {
      const refreshed = await handleUnauthorized()
      if (refreshed) {
        // 重新获取最新token（不再依赖闭包中的旧值）
        const newToken = await getToken()
        if (newToken) {
          // 构建新的fetchOptions（不修改原fetchOptions）
          const retryOptions: RequestInit = {
            ...fetchOptions,
            headers: {
              ...fetchOptions.headers,
              'Authorization': `Bearer ${newToken}`,
            },
          }
          response = await fetch(`${API_URL}${url}`, retryOptions)
        }
      } else {
        await clearAuthAndRedirect()
        throw new ApiError(401, '认证已过期，请重新登录')
      }
    }
    ```
  </action>
  <acceptance_criteria>
    - [ ] refreshLock 单例变量存在于模块级别
    - [ ] handleUnauthorized 返回 Promise<boolean> 并使用锁机制
    - [ ] request 函数中401重试使用重新构建的headers
    - [ ] `pnpm type-check:mobile` 通过，无类型错误
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/shen/Desktop/workspace/apartment-ultra && pnpm type-check:mobile 2>&1 | head -50</automated>
  </verify>
  <done>Token刷新使用单例锁机制，并发401请求不会导致竞态条件</done>
</task>

<task type="auto">
  <name>Task 2: 修复 customers.tsx 类型安全</name>
  <files>mobile/app/(tabs)/customers.tsx, mobile/services/api/index.ts</files>
  <read_first>
    - mobile/app/(tabs)/customers.tsx (full file)
    - mobile/services/api/tenants.ts
    - mobile/services/api/leases.ts
    - packages/api-contract/src/tenants.ts
    - packages/api-contract/src/leases.ts
  </read_first>
  <action>
    修复 customers.tsx 中的 any 类型问题：

    1. 在 mobile/services/api/index.ts 中导出 TenantWithLease 类型（需要先在 tenants.ts 或新建类型文件定义）

    2. 创建联合类型以覆盖API实际返回的数据结构。API返回的租客数据包含 lease 数组（因为租客可能有多个租约）。需要创建：
    ```typescript
    // 在 mobile/services/api/tenants.ts 或新建 mobile/services/api/types.ts
    import type { Tenant } from '@apartment-ultra/api-contract'
    import type { Lease } from './leases'

    /** 租客与租约关联（API实际返回结构） */
    export interface TenantWithLease extends Tenant {
      lease?: Lease[]
    }
    ```

    3. 更新 mobile/services/api/index.ts 导出：
    ```typescript
    export { tenantsApi, type TenantListParams, type TenantWithLease } from './tenants'
    ```

    4. 更新 customers.tsx：
    - 删除 `// eslint-disable-next-line @typescript-eslint/no-explicit-any` 和 `type TenantData = any`
    - 改为导入并使用 `TenantWithLease` 类型
    - 将所有 `TenantData` 替换为 `TenantWithLease`
    - 修复 `router.push('/customers/${tenant.id}' as any)` → 使用正确的路由类型或使用 `router.push(\`/customers/${tenant.id}\`)` (expo-router 支持模板字符串)
    - 修复 `router.push('/customers/new' as any)` → `router.push('/customers/new')`
  </action>
  <acceptance_criteria>
    - [ ] 无 `any` 类型使用（删除 eslint-disable 注释）
    - [ ] 使用 TenantWithLease 类型替代 any
    - [ ] 路由跳转使用类型安全的表达
    - [ ] `pnpm type-check:mobile` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/shen/Desktop/workspace/apartment-ultra && pnpm type-check:mobile 2>&1 | head -50</automated>
  </verify>
  <done>customers.tsx 使用 TenantWithLease 类型，无 any</done>
</task>

<task type="auto">
  <name>Task 3: 修复 reports.tsx 类型安全</name>
  <files>mobile/app/settings/reports.tsx, mobile/services/api/index.ts</files>
  <read_first>
    - mobile/app/settings/reports.tsx (full file)
    - mobile/services/api/reports.ts
    - mobile/services/api/index.ts
    - packages/api-contract/src/reports.ts
  </read_first>
  <action>
    修复 reports.tsx 中的 any 类型问题：

    1. 在 mobile/services/api/index.ts 中确保导出 DashboardOverview 类型：
    ```typescript
    export { reportsApi, type IncomeReportParams, type OccupancyReportParams, type DashboardOverview } from './reports'
    ```

    2. 在 mobile/services/api/reports.ts 中导出 DashboardOverview：
    ```typescript
    export type { IncomeReportParams, OccupancyReportParams, DashboardOverview }
    ```
    （确保从 api-contract 导入的 DashboardOverview 被重新导出）

    3. 更新 reports.tsx：
    - 删除 `// eslint-disable-next-line @typescript-eslint/no-explicit-any` 和 `type OverviewData = any`
    - 导入 `DashboardOverview` 类型：`import { reportsApi } from '@/services/api'`
    - 如果需要显式导入：`import type { DashboardOverview } from '@/services/api'`
    - 将 `useQuery<OverviewData>` 改为 `useQuery<DashboardOverview>`
  </action>
  <acceptance_criteria>
    - [ ] 无 `any` 类型使用（删除 eslint-disable 注释）
    - [ ] 使用 DashboardOverview 类型替代 any
    - [ ] `pnpm type-check:mobile` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/shen/Desktop/workspace/apartment-ultra && pnpm type-check:mobile 2>&1 | head -50</automated>
  </verify>
  <done>reports.tsx 使用 DashboardOverview 类型，无 any</done>
</task>

</tasks>

<verification>
整体验证：
1. `pnpm type-check:mobile` 完全通过
2. `grep -r "any" mobile/app/(tabs)/customers.tsx mobile/app/settings/reports.tsx` 应无 any 类型使用
3. `grep "refreshLock" mobile/services/api/client.ts` 确认单例锁存在
</verification>

<success_criteria>
- Token刷新竞态已修复，使用单例锁机制
- customers.tsx 和 reports.tsx 不再使用 any 类型
- 所有移动端代码通过 TypeScript 类型检查
</success_criteria>

<output>
After completion, create `.planning/phases/07-完善移动端/07-01-SUMMARY.md`
</output>
