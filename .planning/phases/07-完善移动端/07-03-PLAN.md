---
phase: 07-完善移动端
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - mobile/app/(tabs)/_layout.tsx
  - mobile/app/(tabs)/index.tsx
  - mobile/app/(tabs)/bills.tsx
  - mobile/app/(tabs)/profile.tsx
  - mobile/app/(tabs)/customers.tsx
  - mobile/components/bills/BillCard.tsx
  - mobile/components/bills/index.ts
autonomous: true
requirements:
  - MOBILE-05
  - MOBILE-06
---

<objective>
重构Tab导航结构（4 Tab）、拆分bills.tsx、将客户入口内嵌到账单页面。

Purpose: 实现用户决策的Tab导航重构：4个Tab（首页/房源/账单/我的），账单内嵌客户，我的Tab集成设置入口
Output: 重构后的Tab布局、bills.tsx拆分、profile.tsx改版
</objective>

<execution_context>
@/Users/shen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@mobile/app/(tabs)/_layout.tsx
@mobile/app/(tabs)/index.tsx
@mobile/app/(tabs)/bills.tsx
@mobile/app/(tabs)/profile.tsx
@mobile/app/(tabs)/customers.tsx
@.planning/phases/07-完善移动端/07-02-SUMMARY.md (if exists)

<interfaces>
<!-- Current Tab Structure -->
Current tabs (5 tabs):
1. index.tsx - 首页仪表盘
2. properties.tsx - 房源管理
3. customers.tsx - 客户管理 (TO BE REMOVED)
4. bills.tsx - 账单管理
5. profile.tsx - 个人中心

Target Tab Structure (4 tabs):
1. index.tsx - 首页=今日待办+快捷操作
2. properties.tsx - 房源
3. bills.tsx - 账单+内嵌客户入口
4. profile.tsx - 我的=Profile+设置入口

Key decisions from 07-CONTEXT.md:
- Tab导航重构: 4 Tab（首页/房源/账单/我的）
- 首页：今日待办（全部待办：抄表、账单、租约到期等）+ 快捷操作入口
- 账单：内嵌客户入口
- 我的：替代 Profile Tab，设置入口按使用频率排序
- 设置入口：Tab Bar改版，我的Tab集成设置入口
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab导航重构 - 移除客户Tab，内嵌到账单</name>
  <files>
    - mobile/app/(tabs)/_layout.tsx
    - mobile/app/(tabs)/bills.tsx
  </files>
  <read_first>
    - mobile/app/(tabs)/_layout.tsx
    - mobile/app/(tabs)/bills.tsx
    - mobile/app/(tabs)/customers.tsx
  </read_first>
  <action>
    1. 修改 mobile/app/(tabs)/_layout.tsx：
       - 删除 customers Tab（移除 Tabs.Screen name="customers"）
       - 保留 4 个 Tab：index、properties、bills、profile
       - 更新 Tab 名称（如果需要）

    2. 在 bills.tsx 中添加客户入口：
       - 在账单页面顶部或侧边添加"客户"快捷入口按钮
       - 点击跳转到 /customers 页面
       - 样式要简洁，可以是一个小的链接或图标按钮
       - 例如：在页面头部添加"👥 客户"链接，点击跳转到 /customers

    注意：客户功能仍然保留在 /customers 路由，只是从Tab栏移除入口。用户可以通过账单页面进入客户管理。
  </action>
  <acceptance_criteria>
    - [ ] Tab栏只有4个Tab（index、properties、bills、profile）
    - [ ] bills.tsx 包含客户入口
    - [ ] /customers 路由仍然可访问（通过账单页面进入）
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Tabs.Screen" /Users/shen/Desktop/workspace/apartment-ultra/mobile/app/\(tabs\)/_layout.tsx</automated>
  </verify>
  <done>Tab导航从5个减少到4个，客户入口内嵌到账单页面</done>
</task>

<task type="auto">
  <name>Task 2: 拆分 bills.tsx 大文件</name>
  <files>
    - mobile/app/(tabs)/bills.tsx
    - mobile/components/bills/BillCard.tsx
    - mobile/components/bills/FilterTabs.tsx
    - mobile/components/bills/index.ts
  </files>
  <read_first>
    - mobile/app/(tabs)/bills.tsx (完整文件，理解其结构)
  </read_first>
  <action>
    将 363 行的 bills.tsx 按业务功能拆分为子组件：

    1. 创建 mobile/components/bills/ 目录

    2. 识别 bills.tsx 的主要功能区块：
       - BillCard：单个账单卡片（显示金额、状态、操作按钮）
       - FilterTabs：账单状态筛选 Tab（全部/待付/已付/逾期）
       - SearchBar：搜索框

    3. 创建子组件：
       - **BillCard.tsx**: 账单卡片组件，包含收款操作
       - **FilterTabs.tsx**: 状态筛选组件
       - **BillList.tsx**: 账单列表组件（整合搜索+筛选+列表）

    4. 重构 bills.tsx：
       - 导入新组件
       - 保留核心逻辑（useQuery数据获取、mutation提交）
       - 约 200-250 行主文件

    5. 创建 mobile/components/bills/index.ts 统一导出
  </action>
  <acceptance_criteria>
    - [ ] bills.tsx 从 363 行拆分为约 200 行主文件 + 子组件
    - [ ] 子组件位于 mobile/components/bills/ 目录
    - [ ] BillCard 组件包含完整的账单显示和收款操作
    - [ ] `pnpm type-check:mobile` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/shen/Desktop/workspace/apartment-ultra && pnpm type-check:mobile 2>&1 | head -30</automated>
  </verify>
  <done>bills.tsx 拆分为多个子组件</done>
</task>

<task type="auto">
  <name>Task 3: 重构 profile.tsx - 集成设置入口</name>
  <files>
    - mobile/app/(tabs)/profile.tsx
  </files>
  <read_first>
    - mobile/app/(tabs)/profile.tsx
    - mobile/app/settings/fee-configs.tsx (查看设置项)
    - mobile/app/settings/notifications.tsx (查看设置项)
  </read_first>
  <action>
    重构 profile.tsx（改名为"我的"Tab），集成设置入口：

    1. 阅读现有 profile.tsx 结构

    2. 在"我的"页面添加设置入口：
       - 保留现有用户信息区域
       - 添加"设置"入口区域，按使用频率排序：
         1. 费用配置 (fee-configs)
         2. 消息通知 (notifications)
         3. 经营报表 (reports)
         4. 其他设置...

    3. 布局示例：
       ```
       [头像] [用户信息]
       ----------------
       [设置卡片]
       - 费用配置
       - 消息通知
       - 经营报表
       ----------------
       [退出登录]
       ```

    4. 设置项使用 ListItem 组件（如果已创建）或者保持现有样式

    5. 点击设置项跳转到对应页面（router.push）
  </action>
  <acceptance_criteria>
    - [ ] profile.tsx 显示用户信息
    - [ ] profile.tsx 包含设置入口（费用配置、消息通知、经营报表）
    - [ ] Tab名称显示为"我的"
    - [ ] 点击设置项能跳转到对应页面
  </acceptance_criteria>
  <verify>
    <automated>grep -l "费用配置\|消息通知\|经营报表" /Users/shen/Desktop/workspace/apartment-ultra/mobile/app/\(tabs\)/profile.tsx</automated>
  </verify>
  <done>profile.tsx 改名为"我的"，包含设置入口</done>
</task>

</tasks>

<verification>
整体验证：
1. `pnpm type-check:mobile` 完全通过
2. Tab栏只有4个Tab
3. bills.tsx 包含客户入口
4. profile.tsx 包含设置入口
5. `wc -l mobile/app/(tabs)/bills.tsx` 确认行数减少
</verification>

<success_criteria>
- Tab导航从5个减少到4个（首页/房源/账单/我的）
- 账单页面内嵌客户入口
- "我的"页面集成设置入口
- bills.tsx 拆分完成
</success_criteria>

<output>
After completion, create `.planning/phases/07-完善移动端/07-03-SUMMARY.md`
</output>
