---
wave: 1
depends_on: []
requirements_addressed: []
autonomous: false
---

<objective>

统一 /settings 下各子页面的页面标题区结构，消除重复的返回按钮

</objective>

<tasks>

<task>
<read_first>
- tenant-web/src/components/layout/settings-layout.tsx
- tenant-web/src/app/settings/subscription/page.tsx
</read_first>

<action>
删除 `settings/subscription/page.tsx` 中第 64-68 行的重复返回按钮代码：

```tsx
{/* 删除这段——SettingsLayout 已经提供了返回按钮 */}
<Button variant="ghost" size="sm" onClick={() => router.push('/settings')}>
  <ArrowLeft className="mr-2 h-4 w-4" />
  返回
</Button>
```

同时删除第 11 行不再使用的 `ArrowLeft` import。
</action>

<acceptance_criteria>
- settings/subscription/page.tsx 中不再有手写的「返回」按钮
- `import { ArrowLeft, ... }` 中的 ArrowLeft 已被移除（如果 ArrowLeft 未被其他地方使用）
- SettingsLayout 的「返回设置」按钮是该页面唯一的返回导航
</acceptance_criteria>
</task>

<task>
<read_first>
- tenant-web/src/app/settings/team/page.tsx
</read_first>

<action>
将 `settings/team/page.tsx` 的页面标题区（第 247 行附近）从纯 h1 改为统一的标题区结构，与 subscription 页面保持一致：

```tsx
<div className="flex items-center gap-3">
  <Users className="h-8 w-8" />
  <div>
    <h1 className="text-3xl font-bold" data-testid={TEAM_SETTINGS.HEADING}>团队设置</h1>
    <p className="text-muted-foreground">管理组织成员和权限</p>
  </div>
</div>
```

需要添加 `Users` 图标的 import（从 lucide-react）。
</action>

<acceptance_criteria>
- settings/team/page.tsx 页面标题区包含图标 + h1 + 描述的结构
- Users 图标从 lucide-react 正确导入
- data-testid 保持不变
</acceptance_criteria>
</task>

<task>
<read_first>
- tenant-web/src/app/settings/notifications/page.tsx
</read_first>

<action>
将 `settings/notifications/page.tsx` 的页面标题区（第 186-194 行附近）改为与 subscription 页面一致的标题区结构：

```tsx
<div className="flex items-center gap-3">
  <BellRing className="h-8 w-8" />
  <div>
    <h1 className="text-3xl font-bold">租客消息触达</h1>
    <p className="text-muted-foreground">
      当前第一期仅接入短信通道，覆盖账单生成、到期前提醒和逾期催缴。
    </p>
  </div>
</div>
```

BellRing 已在此文件中导入（第 31 行）。
</action>

<acceptance_criteria>
- settings/notifications/page.tsx 页面标题区包含图标 + h1 + 描述的结构
- BellRing 图标保持使用（已有 import）
- 描述文本保持不变
</acceptance_criteria>
</task>

<task>
<read_first>
- tenant-web/src/app/settings/page.tsx
- tenant-web/src/app/settings/permissions/page.tsx
</read_first>

<action>
检查 `settings/page.tsx`（设置首页）和 `settings/permissions/page.tsx` 的页面标题区结构，确保它们也遵循统一的图标+标题+描述模式（如果适用）。

如果这些页面已有类似结构则无需修改；如果缺少图标或描述，根据页面性质决定是否补充。
</action>

<acceptance_criteria>
- settings/page.tsx 标题区结构清晰
- settings/permissions/page.tsx 标题区结构与同级别页面一致
</acceptance_criteria>
</task>

</tasks>

<verification>

1. 运行 `pnpm type-check` 确认无类型错误
2. 运行 `pnpm lint` 确认无 lint 错误
3. 人工验证：访问 /settings/team、/settings/notifications、/settings/subscription，确认：
   - 返回按钮正常工作
   - 面包屑正常显示
   - 页面标题区图标+标题+描述结构统一

</verification>

<must_haves>

- 各 settings 子页面（team、notifications、subscription）标题区结构统一
- 无重复的返回按钮（SettingsLayout 提供统一的返回导航）
- subscription 页面不再有手写返回按钮

</must_haves>
