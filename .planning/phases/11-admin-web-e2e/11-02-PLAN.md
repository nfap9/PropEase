---
phase: 11-admin-web-e2e
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - e2e/pages/admin/overview-page.ts
  - e2e/pages/admin/organizations-page.ts
  - e2e/pages/admin/plans-page.ts
  - e2e/pages/admin/registered-users-page.ts
  - e2e/pages/admin/roles-page.ts
  - e2e/pages/admin/users-page.ts
  - e2e/pages/admin/subscriptions-page.ts
  - e2e/pages/admin/brand-page.ts
  - e2e/pages/admin/pricing-page.ts
  - e2e/pages/admin/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "9 个 admin 页面各有独立 Page Object 类"
    - "每个 Page Object 继承 BaseAdminPage"
    - "所有 locators 使用 testids.ts 中的 ADMIN_* 常量"
    - "每个 Page Object 有 load() 方法用于页面加载"
  artifacts:
    - path: "e2e/pages/admin/overview-page.ts"
      provides: "OverviewPage class"
      min_lines: 40
    - path: "e2e/pages/admin/organizations-page.ts"
      provides: "OrganizationsPage class"
      min_lines: 40
    - path: "e2e/pages/admin/plans-page.ts"
      provides: "PlansPage class"
      min_lines: 40
    - path: "e2e/pages/admin/registered-users-page.ts"
      provides: "RegisteredUsersPage class"
      min_lines: 40
    - path: "e2e/pages/admin/roles-page.ts"
      provides: "RolesPage class"
      min_lines: 40
    - path: "e2e/pages/admin/users-page.ts"
      provides: "UsersPage class"
      min_lines: 40
    - path: "e2e/pages/admin/subscriptions-page.ts"
      provides: "SubscriptionsPage class"
      min_lines: 40
    - path: "e2e/pages/admin/brand-page.ts"
      provides: "BrandPage class"
      min_lines: 40
    - path: "e2e/pages/admin/pricing-page.ts"
      provides: "PricingPage class"
      min_lines: 40
  key_links:
    - from: "e2e/pages/admin/*.ts"
      to: "e2e/pages/admin/base-admin-page.ts"
      via: "extends BaseAdminPage"
      pattern: "extends BaseAdminPage"
    - from: "e2e/pages/admin/*.ts"
      to: "e2e/testids.ts"
      via: "import ADMIN_* constants"
      pattern: "from '../../testids'"
---

<objective>
为所有 admin 页面创建 Page Object 类，封装页面 locators 和操作方法。覆盖：概览、组织管理、服务配置、注册用户、运营角色、运营账号、订阅管理、品牌配置、额度定价共 9 个页面。
</objective>

<execution_context>
@/Users/shen/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@e2e/pages/admin/base-admin-page.ts
@e2e/testids.ts
@e2e/pages/base-page.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建概览页和组织管理页 Page Object</name>
  <files>e2e/pages/admin/overview-page.ts, e2e/pages/admin/organizations-page.ts</files>
  <action>
创建两个 Page Object 文件：

**e2e/pages/admin/overview-page.ts** - 概览页：
```typescript
import { Page } from '@playwright/test';
import { BaseAdminPage } from './base-admin-page';
import { ADMIN, ADMIN_LOGIN } from '../../testids';

export class OverviewPage extends BaseAdminPage {
  readonly heading = this.page.locator(`[data-testid="${ADMIN.OVERVIEW_HEADING}"]`);
  readonly orgCountCard = this.page.locator(`[data-testid="${ADMIN.ORG_COUNT}"]`);
  readonly userCountCard = this.page.locator(`[data-testid="${ADMIN.USER_COUNT}"]`);
  readonly apartmentCountCard = this.page.locator(`[data-testid="${ADMIN.APARTMENT_COUNT}"]`);
  readonly roomCountCard = this.page.locator(`[data-testid="${ADMIN.ROOM_COUNT}"]`);
  readonly activeSubscriptionCard = this.page.locator(`[data-testid="${ADMIN.ACTIVE_SUBSCRIPTION_COUNT}"]`);
  readonly logoutButton = this.page.locator(`[data-testid="${ADMIN.LOGOUT_BUTTON}"]`);

  constructor(page: Page) {
    super(page);
  }

  async load(): Promise<void> {
    await this.goto('/');
    await this.waitForHeading(ADMIN.OVERVIEW_HEADING);
  }

  async getStatValue(testId: string): Promise<string> {
    const card = this.page.locator(`[data-testid="${testId}"]`);
    await card.waitFor({ state: 'visible', timeout: 10000 });
    return (await card.textContent()) ?? '';
  }

  async logout(): Promise<void> {
    await this.logoutButton.click();
    await this.waitForLoginPage();
  }
}
```

**e2e/pages/admin/organizations-page.ts** - 组织管理页：
```typescript
import { Page } from '@playwright/test';
import { BaseAdminPage } from './base-admin-page';
import { ADMIN_ORGANIZATIONS } from '../../testids';

export class OrganizationsPage extends BaseAdminPage {
  readonly heading = this.page.locator(`[data-testid="${ADMIN_ORGANIZATIONS.HEADING}"]`);
  readonly list = this.page.locator(`[data-testid="${ADMIN_ORGANIZATIONS.LIST}"]`);
  readonly enableButton = this.page.locator(`[data-testid="${ADMIN_ORGANIZATIONS.ENABLE_BUTTON}"]`);
  readonly disableButton = this.page.locator(`[data-testid="${ADMIN_ORGANIZATIONS.DISABLE_BUTTON}"]`);

  constructor(page: Page) {
    super(page);
  }

  async load(): Promise<void> {
    await this.goto('/admin/organizations');
    await this.waitForHeading(ADMIN_ORGANIZATIONS.HEADING);
  }

  async getListCount(): Promise<number> {
    await this.list.waitFor({ state: 'visible', timeout: 10000 });
    return this.list.locator('> *').count();
  }

  async isListVisible(): Promise<boolean> {
    return this.list.isVisible();
  }
}
```
</action>
  <verify>
grep -n "class OverviewPage" e2e/pages/admin/overview-page.ts
grep -n "class OrganizationsPage" e2e/pages/admin/organizations-page.ts
grep -n "extends BaseAdminPage" e2e/pages/admin/overview-page.ts
</verify>
  <done>OverviewPage 和 OrganizationsPage 创建完成</done>
</task>

<task type="auto">
  <name>Task 2: 创建服务配置页、注册用户页、运营角色页 Page Object</name>
  <files>e2e/pages/admin/plans-page.ts, e2e/pages/admin/registered-users-page.ts, e2e/pages/admin/roles-page.ts</files>
  <action>
创建三个 Page Object 文件：

**e2e/pages/admin/plans-page.ts** - 服务配置页：
```typescript
import { Page } from '@playwright/test';
import { BaseAdminPage } from './base-admin-page';
import { ADMIN_PLANS } from '../../testids';

export class PlansPage extends BaseAdminPage {
  readonly heading = this.page.locator(`[data-testid="${ADMIN_PLANS.HEADING}"]`);
  readonly list = this.page.locator(`[data-testid="${ADMIN_PLANS.LIST}"]`);
  readonly createButton = this.page.locator(`[data-testid="${ADMIN_PLANS.CREATE_BUTTON}"]`);
  readonly createDialog = this.page.locator(`[data-testid="${ADMIN_PLANS.CREATE_DIALOG}"]`);

  constructor(page: Page) {
    super(page);
  }

  async load(): Promise<void> {
    await this.goto('/admin/plans');
    await this.waitForHeading(ADMIN_PLANS.HEADING);
  }

  async clickCreate(): Promise<void> {
    await this.createButton.click();
    await this.createDialog.waitFor({ state: 'visible', timeout: 10000 });
  }
}
```

**e2e/pages/admin/registered-users-page.ts** - 注册用户页：
```typescript
import { Page } from '@playwright/test';
import { BaseAdminPage } from './base-admin-page';
import { ADMIN_REGISTERED_USERS } from '../../testids';

export class RegisteredUsersPage extends BaseAdminPage {
  readonly heading = this.page.locator(`[data-testid="${ADMIN_REGISTERED_USERS.HEADING}"]`);
  readonly list = this.page.locator(`[data-testid="${ADMIN_REGISTERED_USERS.LIST}"]`);
  readonly searchInput = this.page.locator(`[data-testid="${ADMIN_REGISTERED_USERS.SEARCH_INPUT}"]`);

  constructor(page: Page) {
    super(page);
  }

  async load(): Promise<void> {
    await this.goto('/admin/registered-users');
    await this.waitForHeading(ADMIN_REGISTERED_USERS.HEADING);
  }

  async search(keyword: string): Promise<void> {
    await this.searchInput.fill(keyword);
    await this.searchInput.press('Enter');
  }
}
```

**e2e/pages/admin/roles-page.ts** - 运营角色页：
```typescript
import { Page } from '@playwright/test';
import { BaseAdminPage } from './base-admin-page';
import { ADMIN_ROLES } from '../../testids';

export class RolesPage extends BaseAdminPage {
  readonly heading = this.page.locator(`[data-testid="${ADMIN_ROLES.HEADING}"]`);
  readonly list = this.page.locator(`[data-testid="${ADMIN_ROLES.LIST}"]`);
  readonly createButton = this.page.locator(`[data-testid="${ADMIN_ROLES.CREATE_BUTTON}"]`);
  readonly createDialog = this.page.locator(`[data-testid="${ADMIN_ROLES.CREATE_DIALOG}"]`);

  constructor(page: Page) {
    super(page);
  }

  async load(): Promise<void> {
    await this.goto('/admin/roles');
    await this.waitForHeading(ADMIN_ROLES.HEADING);
  }
}
```
</action>
  <verify>
grep -n "class PlansPage" e2e/pages/admin/plans-page.ts
grep -n "class RegisteredUsersPage" e2e/pages/admin/registered-users-page.ts
grep -n "class RolesPage" e2e/pages/admin/roles-page.ts
</verify>
  <done>PlansPage、RegisteredUsersPage、RolesPage 创建完成</done>
</task>

<task type="auto">
  <name>Task 3: 创建运营账号、订阅管理、品牌配置、额度定价 Page Object</name>
  <files>e2e/pages/admin/users-page.ts, e2e/pages/admin/subscriptions-page.ts, e2e/pages/admin/brand-page.ts, e2e/pages/admin/pricing-page.ts</files>
  <action>
创建四个 Page Object 文件：

**e2e/pages/admin/users-page.ts** - 运营账号页：
```typescript
import { Page } from '@playwright/test';
import { BaseAdminPage } from './base-admin-page';
import { ADMIN_USERS } from '../../testids';

export class UsersPage extends BaseAdminPage {
  readonly heading = this.page.locator(`[data-testid="${ADMIN_USERS.HEADING}"]`);
  readonly list = this.page.locator(`[data-testid="${ADMIN_USERS.LIST}"]`);
  readonly createButton = this.page.locator(`[data-testid="${ADMIN_USERS.CREATE_BUTTON}"]`);
  readonly createDialog = this.page.locator(`[data-testid="${ADMIN_USERS.CREATE_DIALOG}"]`);

  constructor(page: Page) {
    super(page);
  }

  async load(): Promise<void> {
    await this.goto('/admin/users');
    await this.waitForHeading(ADMIN_USERS.HEADING);
  }
}
```

**e2e/pages/admin/subscriptions-page.ts** - 订阅管理页：
```typescript
import { Page } from '@playwright/test';
import { BaseAdminPage } from './base-admin-page';
import { ADMIN_SUBSCRIPTIONS } from '../../testids';

export class SubscriptionsPage extends BaseAdminPage {
  readonly heading = this.page.locator(`[data-testid="${ADMIN_SUBSCRIPTIONS.HEADING}"]`);
  readonly list = this.page.locator(`[data-testid="${ADMIN_SUBSCRIPTIONS.LIST}"]`);
  readonly statusFilter = this.page.locator(`[data-testid="${ADMIN_SUBSCRIPTIONS.STATUS_FILTER}"]`);
  readonly planFilter = this.page.locator(`[data-testid="${ADMIN_SUBSCRIPTIONS.PLAN_FILTER}"]`);

  constructor(page: Page) {
    super(page);
  }

  async load(): Promise<void> {
    await this.goto('/admin/subscriptions');
    await this.waitForHeading(ADMIN_SUBSCRIPTIONS.HEADING);
  }
}
```

**e2e/pages/admin/brand-page.ts** - 品牌配置页：
```typescript
import { Page } from '@playwright/test';
import { BaseAdminPage } from './base-admin-page';
import { ADMIN_BRAND } from '../../testids';

export class BrandPage extends BaseAdminPage {
  readonly heading = this.page.locator(`[data-testid="${ADMIN_BRAND.HEADING}"]`);
  readonly nameInput = this.page.locator(`[data-testid="${ADMIN_BRAND.NAME_INPUT}"]`);
  readonly logoInput = this.page.locator(`[data-testid="${ADMIN_BRAND.LOGO_INPUT}"]`);
  readonly saveButton = this.page.locator(`[data-testid="${ADMIN_BRAND.SAVE_BUTTON}"]`);

  constructor(page: Page) {
    super(page);
  }

  async load(): Promise<void> {
    await this.goto('/admin/brand');
    await this.waitForHeading(ADMIN_BRAND.HEADING);
  }
}
```

**e2e/pages/admin/pricing-page.ts** - 额度定价页：
```typescript
import { Page } from '@playwright/test';
import { BaseAdminPage } from './base-admin-page';
import { ADMIN_PRICING } from '../../testids';

export class PricingPage extends BaseAdminPage {
  readonly heading = this.page.locator(`[data-testid="${ADMIN_PRICING.HEADING}"]`);
  readonly priceInput = this.page.locator(`[data-testid="${ADMIN_PRICING.PRICE_INPUT}"]`);
  readonly saveButton = this.page.locator(`[data-testid="${ADMIN_PRICING.SAVE_BUTTON}"]`);

  constructor(page: Page) {
    super(page);
  }

  async load(): Promise<void> {
    await this.goto('/admin/pricing');
    await this.waitForHeading(ADMIN_PRICING.HEADING);
  }
}
```
</action>
  <verify>
grep -n "class UsersPage" e2e/pages/admin/users-page.ts
grep -n "class SubscriptionsPage" e2e/pages/admin/subscriptions-page.ts
grep -n "class BrandPage" e2e/pages/admin/brand-page.ts
grep -n "class PricingPage" e2e/pages/admin/pricing-page.ts
</verify>
  <done>UsersPage、SubscriptionsPage、BrandPage、PricingPage 创建完成</done>
</task>

</tasks>

<verification>
- 所有 9 个 admin 页面都有对应的 Page Object 类
- 每个 Page Object 继承 BaseAdminPage
- 所有 locators 使用 testids.ts 中的常量
</verification>

<success_criteria>
- OverviewPage、OrganizationsPage、PlansPage、RegisteredUsersPage、RolesPage、UsersPage、SubscriptionsPage、BrandPage、PricingPage 共 9 个 Page Object 创建完成
- 每个 Page Object 有 load() 方法用于页面加载
- 所有元素使用 testids.ts 中的常量
</success_criteria>

<output>
After completion, create `.planning/phases/11-admin-web-e2e/11-02-SUMMARY.md`
</output>
