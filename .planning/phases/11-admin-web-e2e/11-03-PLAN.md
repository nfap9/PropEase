---
phase: 11-admin-web-e2e
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - e2e/admin/overview.spec.ts
  - e2e/admin/organizations.spec.ts
  - e2e/admin/plans.spec.ts
  - e2e/admin/users.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "4 个重写的 spec 文件不使用 waitForTimeout"
    - "所有 spec 使用 import { test, expect } from '../fixtures'"
    - "所有 spec 使用 { adminPage } fixture 而非 { page }"
    - "所有 spec 使用 Page Objects 封装页面操作"
  artifacts:
    - path: "e2e/admin/overview.spec.ts"
      provides: "重写后的概览页测试"
      min_lines: 30
      contains: "OverviewPage"
    - path: "e2e/admin/organizations.spec.ts"
      provides: "重写后的组织管理测试"
      min_lines: 30
      contains: "OrganizationsPage"
    - path: "e2e/admin/plans.spec.ts"
      provides: "重写后的服务配置测试"
      min_lines: 40
      contains: "PlansPage"
    - path: "e2e/admin/users.spec.ts"
      provides: "重写后的注册用户测试"
      min_lines: 40
      contains: "RegisteredUsersPage"
  key_links:
    - from: "e2e/admin/*.spec.ts"
      to: "e2e/pages/admin/*.ts"
      via: "import Page Object classes"
      pattern: "from '../pages/admin/"
    - from: "e2e/admin/*.spec.ts"
      to: "e2e/fixtures.ts"
      via: "import test, expect from fixtures"
      pattern: "from '../fixtures'"
---

<objective>
重写现有 admin E2E 测试（概览、组织管理、服务配置、运营账号），消除 waitForTimeout，使用 Page Objects 和 fixtures，添加真实 CRUD 操作验证。
</objective>

<execution_context>
@/Users/shen/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@e2e/pages/admin/overview-page.ts
@e2e/pages/admin/organizations-page.ts
@e2e/pages/admin/plans-page.ts
@e2e/pages/admin/users-page.ts
@e2e/fixtures.ts
@e2e/helpers/auth.ts
@e2e/helpers/ui.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 overview.spec.ts 和 organizations.spec.ts</name>
  <files>e2e/admin/overview.spec.ts, e2e/admin/organizations.spec.ts</files>
  <action>
**重写 e2e/admin/overview.spec.ts**：

删除现有的直接 import `@playwright/test` 的写法，改为使用 fixtures.ts 导出：

```typescript
import { test, expect } from '../fixtures';
import { OverviewPage } from '../pages/admin/overview-page';
import { ADMIN } from '../testids';

test.describe('运营后台概览页', () => {
  let overviewPage: OverviewPage;

  test.beforeEach(async ({ adminPage }) => {
    overviewPage = new OverviewPage(adminPage);
    await overviewPage.load();
  });

  test('显示统计数据卡片', async () => {
    // 等待卡片可见
    await overviewPage.orgCountCard.waitFor({ state: 'visible', timeout: 10000 });
    await overviewPage.userCountCard.waitFor({ state: 'visible', timeout: 10000 });

    // 验证卡片内容非空
    const orgCount = await overviewPage.getStatValue(ADMIN.ORG_COUNT);
    expect(orgCount.trim().length).toBeGreaterThan(0);
  });

  test('显示房间数统计', async () => {
    await overviewPage.apartmentCountCard.waitFor({ state: 'visible', timeout: 10000 });
    await overviewPage.roomCountCard.waitFor({ state: 'visible', timeout: 10000 });
  });

  test('退出登录成功', async () => {
    await overviewPage.logout();
    await expect(overviewPage.page).toHaveURL(/admin\/login/);
  });
});
```

**重写 e2e/admin/organizations.spec.ts**：

```typescript
import { test, expect } from '../fixtures';
import { OrganizationsPage } from '../pages/admin/organizations-page';
import { ADMIN_ORGANIZATIONS } from '../testids';

test.describe('组织管理页面', () => {
  let orgPage: OrganizationsPage;

  test.beforeEach(async ({ adminPage }) => {
    orgPage = new OrganizationsPage(adminPage);
    await orgPage.load();
  });

  test('显示组织列表', async () => {
    await expect(orgPage.list).toBeVisible();
    await expect(orgPage.heading).toBeVisible();
  });

  test('组织列表可以加载数据', async () => {
    const count = await orgPage.getListCount();
    expect(count).toBeGreaterThanOrEqual(0);
  });
});
```

关键改动：
- 使用 `import { test, expect } from '../fixtures'` 而非直接 `@playwright/test`
- 使用 `{ adminPage }` fixture 而非 `{ page }`
- 消除所有 `waitForTimeout`，改用 `waitFor({ state: 'visible' })`
- 使用 Page Object 封装页面操作
</action>
  <verify>
grep -n "waitForTimeout" e2e/admin/overview.spec.ts
grep -n "waitForTimeout" e2e/admin/organizations.spec.ts
grep -n "from '../fixtures'" e2e/admin/overview.spec.ts
grep -n "adminPage" e2e/admin/overview.spec.ts
</verify>
  <done>overview.spec.ts 和 organizations.spec.ts 使用新架构重写，无 waitForTimeout</done>
</task>

<task type="auto">
  <name>Task 2: 重写 plans.spec.ts 和 users.spec.ts</name>
  <files>e2e/admin/plans.spec.ts, e2e/admin/users.spec.ts</files>
  <action>
**重写 e2e/admin/plans.spec.ts**：

```typescript
import { test, expect } from '../fixtures';
import { PlansPage } from '../pages/admin/plans-page';
import { ADMIN_PLANS } from '../testids';

test.describe('服务配置页面', () => {
  let plansPage: PlansPage;

  test.beforeEach(async ({ adminPage }) => {
    plansPage = new PlansPage(adminPage);
    await plansPage.load();
  });

  test('显示服务列表', async () => {
    await expect(plansPage.list).toBeVisible();
    await expect(plansPage.heading).toBeVisible();
  });

  test('显示创建服务按钮', async () => {
    await expect(plansPage.createButton).toBeVisible();
    await expect(plansPage.createButton).toBeEnabled();
  });

  test('点击创建按钮显示弹窗', async () => {
    await plansPage.clickCreate();
    await expect(plansPage.createDialog).toBeVisible();
  });

  test('创建服务弹窗包含必要元素', async () => {
    await plansPage.clickCreate();
    await expect(plansPage.createDialog).toBeVisible();
    // 弹窗中应有关闭按钮
    const closeButton = plansPage.createDialog.locator('button[aria-label="关闭"], [data-testid="common-close-btn"]');
    await expect(closeButton.first()).toBeVisible();
  });
});
```

**重写 e2e/admin/users.spec.ts**（注意：users.spec.ts 测试的是注册用户管理，非运营账号）：

```typescript
import { test, expect } from '../fixtures';
import { RegisteredUsersPage } from '../pages/admin/registered-users-page';
import { ADMIN_REGISTERED_USERS } from '../testids';

test.describe('注册用户管理页面', () => {
  let usersPage: RegisteredUsersPage;

  test.beforeEach(async ({ adminPage }) => {
    usersPage = new RegisteredUsersPage(adminPage);
    await usersPage.load();
  });

  test('显示用户列表', async () => {
    await expect(usersPage.list).toBeVisible();
    await expect(usersPage.heading).toBeVisible();
  });

  test('搜索框可输入', async () => {
    await expect(usersPage.searchInput).toBeVisible();
    await usersPage.search('138');
    // 等待搜索结果
    await usersPage.list.waitFor({ state: 'visible', timeout: 10000 });
  });

  test('清空搜索显示所有用户', async () => {
    await usersPage.search('138');
    await usersPage.search('');
    await usersPage.list.waitFor({ state: 'visible', timeout: 10000 });
  });
});
```

关键改动：
- 使用 `import { test, expect } from '../fixtures'`
- 使用 `{ adminPage }` fixture
- 消除所有 `waitForTimeout`
- 使用 Page Object 封装
</action>
  <verify>
grep -n "waitForTimeout" e2e/admin/plans.spec.ts
grep -n "waitForTimeout" e2e/admin/users.spec.ts
grep -n "from '../fixtures'" e2e/admin/plans.spec.ts
grep -n "RegisteredUsersPage" e2e/admin/users.spec.ts
</verify>
  <done>plans.spec.ts 和 users.spec.ts 使用新架构重写，无 waitForTimeout</done>
</task>

</tasks>

<verification>
- 所有 4 个重写的 spec 文件不包含 waitForTimeout
- 所有 spec 使用 `import { test, expect } from '../fixtures'`
- 所有 spec 使用 `{ adminPage }` fixture 而非 `{ page }`
- 所有 spec 使用 Page Objects 封装页面操作
</verification>

<success_criteria>
- overview.spec.ts、organizations.spec.ts、plans.spec.ts、users.spec.ts 全部使用新架构
- 消除所有 waitForTimeout，改用 Playwright 自动等待
- 使用 fixtures.ts 导出的 test 和 expect
- 使用 adminPage fixture 而非直接使用 page
</success_criteria>

<output>
After completion, create `.planning/phases/11-admin-web-e2e/11-03-SUMMARY.md`
</output>
