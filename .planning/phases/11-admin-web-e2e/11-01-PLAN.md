---
phase: 11-admin-web-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/fixtures.ts
  - playwright.config.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "adminPage fixture 使用 worker scope 复用登录状态"
    - "playwright.config.ts 同时启动 tenant-web (3000) 和 admin-web (3001)"
    - "BaseAdminPage 继承 BasePage 并提供 admin 专用 goto/waitForHeading 方法"
  artifacts:
    - path: "e2e/fixtures.ts"
      provides: "adminPage fixture with worker scope and storageState"
      min_lines: 140
    - path: "playwright.config.ts"
      provides: "dual webServer configuration"
      contains: "dev:admin"
    - path: "e2e/pages/admin/base-admin-page.ts"
      provides: "BaseAdminPage class extending BasePage"
      min_lines: 50
  key_links:
    - from: "fixtures.ts"
      to: "helpers/auth.ts"
      via: "adminLogin function import"
      pattern: "adminLogin"
    - from: "playwright.config.ts"
      to: "e2e/admin"
      via: "testDir ./e2e"
      pattern: "testDir"
---

<objective>
建立 admin E2E 测试基础设施：扩展 fixtures.ts 的 adminPage fixture 为 worker 级复用、更新 playwright.config.ts 支持双服务启动、创建 Page Object 目录结构和 BaseAdminPage 基类。
</objective>

<execution_context>
@/Users/shen/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@e2e/fixtures.ts
@e2e/helpers/auth.ts
@playwright.config.ts
@e2e/pages/base-page.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 fixtures.ts adminPage 为 worker 级 fixture</name>
  <files>e2e/fixtures.ts</files>
  <action>
扩展 fixtures.ts 中的 adminPage fixture，改为 worker 级 scope 实现状态复用：

1. 在 adminPage fixture 中添加 storageState 复用逻辑：
   - 状态文件路径: `e2e/results/.auth/admin.json`
   - 在 fixture setup 时检查该文件是否存在
   - 如果存在则使用 `context.storageState({ path })` 复用
   - 如果不存在则执行 adminLogin 登录后保存状态
   - scope 改为 `scope: 'worker'`（整个进程共享）

2. 确保 adminLogin 函数支持检测已登录状态（跳过重复登录）

修改后的 adminPage fixture 应为：
```typescript
adminPage: [
  async ({ page, context }, use) => {
    const storageStatePath = 'e2e/results/.auth/admin.json';
    try {
      const fs = await import('fs');
      if (fs.existsSync(storageStatePath)) {
        await context.storageState({ path: storageStatePath });
      }
    } catch {}
    await adminLogin(page);
    await use(page);
    await context.storageState({ path: storageStatePath });
  },
  { scope: 'worker' },
],
```

3. 确保 adminLogin 函数（e2e/helpers/auth.ts）先检查是否已认证，如已认证则跳过登录
</action>
  <verify>
grep -n "scope: 'worker'" e2e/fixtures.ts
grep -n "adminPage:" e2e/fixtures.ts | head -5
grep -n "storageState" e2e/fixtures.ts
</verify>
  <done>adminPage fixture 改为 worker 级，支持跨测试复用登录状态</done>
</task>

<task type="auto">
  <name>Task 2: 更新 playwright.config.ts 支持双服务启动</name>
  <files>playwright.config.ts</files>
  <action>
更新 playwright.config.ts 的 webServer 配置，从单一服务改为数组配置支持同时启动 tenant-web 和 admin-web：

将原有的单一 webServer 对象：
```typescript
webServer: {
  command: 'pnpm dev:web',
  url: 'http://localhost:3000',
  reuseExistingServer: !process.env.CI,
  timeout: 120000,
},
```

改为 webServer 数组配置：
```typescript
webServer: [
  {
    command: 'pnpm dev:web',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120000,
  },
  {
    command: 'pnpm dev:admin',
    url: 'http://localhost:3001',
    reuseExistingServer: !process.env.CI,
    timeout: 120000,
  },
],
```

同时在 use 配置中添加 E2E_ADMIN_BASE_URL 环境变量：
```typescript
E2E_ADMIN_BASE_URL: process.env.E2E_ADMIN_BASE_URL || 'http://localhost:3001',
```
</action>
  <verify>
grep -n "webServer:" playwright.config.ts
grep -n "E2E_ADMIN_BASE_URL" playwright.config.ts
grep -n "dev:admin" playwright.config.ts
</verify>
  <done>playwright.config.ts 支持同时启动 tenant-web (3000) 和 admin-web (3001)</done>
</task>

<task type="auto">
  <name>Task 3: 创建 e2e/pages/admin/ 目录和 BaseAdminPage 基类</name>
  <files>e2e/pages/admin/base-admin-page.ts</files>
  <action>
在 e2e/pages/admin/ 目录下创建 BaseAdminPage 基类，扩展现有的 BasePage：

1. 创建目录: e2e/pages/admin/
2. 创建 base-admin-page.ts，继承 BasePage，提供 admin 通用能力：

```typescript
import { Page } from '@playwright/test';
import { BasePage } from '../base-page';
import { ADMIN_LOGIN } from '../../testids';

export class BaseAdminPage extends BasePage {
  protected adminBaseUrl: string;

  constructor(page: Page) {
    super(page);
    this.adminBaseUrl = process.env.E2E_ADMIN_BASE_URL || 'http://localhost:3001';
  }

  async goto(path: string = ''): Promise<void> {
    await this.page.goto(`${this.adminBaseUrl}${path}`);
  }

  async isOnPage(headingTestId: string): Promise<boolean> {
    return this.page.locator(`[data-testid="${headingTestId}"]`).isVisible();
  }

  async waitForHeading(headingTestId: string): Promise<void> {
    await this.page.waitForSelector(
      `[data-testid="${headingTestId}"]`,
      { timeout: 10000 }
    );
  }

  async waitForLoginPage(): Promise<void> {
    await this.page.waitForSelector(
      `[data-testid="${ADMIN_LOGIN.PAGE}"]`,
      { timeout: 10000 }
    );
  }

  async isLoggedIn(): Promise<boolean> {
    return this.page.evaluate(() => {
      return localStorage.getItem('admin_access_token') !== null;
    });
  }
}
```

3. 创建 index.ts 导出：
```typescript
export { BaseAdminPage } from './base-admin-page';
```

4. 确保 e2e/pages/index.ts 能导出 admin 子模块
</action>
  <verify>
ls -la e2e/pages/admin/
grep -n "class BaseAdminPage" e2e/pages/admin/base-admin-page.ts
grep -n "extends BasePage" e2e/pages/admin/base-admin-page.ts
</verify>
  <done>BaseAdminPage 基类创建完成，继承 BasePage 并添加 admin 专用方法</done>
</task>

</tasks>

<verification>
- fixtures.ts 中 adminPage 使用 worker scope 和 storageState 复用
- playwright.config.ts 配置了双 webServer (3000 + 3001)
- e2e/pages/admin/base-admin-page.ts 继承 BasePage 并提供 admin 通用能力
</verification>

<success_criteria>
- adminPage fixture scope 为 'worker'，支持跨测试复用登录状态
- 运行 admin E2E 测试时同时启动 tenant-web 和 admin-web
- BaseAdminPage 提供 goto、waitForHeading、isLoggedIn 等 admin 通用方法
</success_criteria>

<output>
After completion, create `.planning/phases/11-admin-web-e2e/11-01-SUMMARY.md`
</output>
