---
phase: 11-admin-web-e2e
plan: 04
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - e2e/admin/roles.spec.ts
  - e2e/admin/subscriptions.spec.ts
  - e2e/admin/brand.spec.ts
  - e2e/admin/pricing.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "4 个新 spec 文件使用 import { test, expect } from '../fixtures'"
    - "4 个新 spec 使用 { adminPage } fixture"
    - "4 个新 spec 无 waitForTimeout"
    - "每个页面至少有 3 个测试用例"
  artifacts:
    - path: "e2e/admin/roles.spec.ts"
      provides: "运营角色页测试"
      min_lines: 35
      contains: "RolesPage"
    - path: "e2e/admin/subscriptions.spec.ts"
      provides: "订阅管理页测试"
      min_lines: 35
      contains: "SubscriptionsPage"
    - path: "e2e/admin/brand.spec.ts"
      provides: "品牌配置页测试"
      min_lines: 35
      contains: "BrandPage"
    - path: "e2e/admin/pricing.spec.ts"
      provides: "额度定价页测试"
      min_lines: 35
      contains: "PricingPage"
  key_links:
    - from: "e2e/admin/*.spec.ts"
      to: "e2e/pages/admin/*.ts"
      via: "import Page Object classes"
      pattern: "from '../pages/admin/"
    - from: "e2e/admin/*.spec.ts"
      to: "e2e/fixtures.ts"
      via: "import test, expect from fixtures"
      pattern: "from '../fixtures'"
---

<objective>
为运营后台新增 4 个页面的 E2E 测试：运营角色、订阅管理、品牌配置、额度定价。采用新架构（fixtures + Page Objects），无 waitForTimeout。
</objective>

<execution_context>
@/Users/shen/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@e2e/pages/admin/roles-page.ts
@e2e/pages/admin/subscriptions-page.ts
@e2e/pages/admin/brand-page.ts
@e2e/pages/admin/pricing-page.ts
@e2e/fixtures.ts
@e2e/testids.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 roles.spec.ts 和 subscriptions.spec.ts</name>
  <files>e2e/admin/roles.spec.ts, e2e/admin/subscriptions.spec.ts</files>
  <action>
**创建 e2e/admin/roles.spec.ts** - 运营角色页：

```typescript
import { test, expect } from '../fixtures';
import { RolesPage } from '../pages/admin/roles-page';
import { ADMIN_ROLES } from '../testids';

test.describe('运营角色页面', () => {
  let rolesPage: RolesPage;

  test.beforeEach(async ({ adminPage }) => {
    rolesPage = new RolesPage(adminPage);
    await rolesPage.load();
  });

  test('显示角色列表', async () => {
    await expect(rolesPage.heading).toBeVisible();
    await expect(rolesPage.list).toBeVisible();
  });

  test('显示新建角色按钮', async () => {
    await expect(rolesPage.createButton).toBeVisible();
    await expect(rolesPage.createButton).toBeEnabled();
  });

  test('点击新建角色按钮显示弹窗', async () => {
    await rolesPage.createButton.click();
    await expect(rolesPage.createDialog).toBeVisible();
  });

  test('新建角色弹窗有关闭按钮', async () => {
    await rolesPage.createButton.click();
    await expect(rolesPage.createDialog).toBeVisible();
    // 验证关闭按钮存在
    const closeButton = rolesPage.createDialog.locator('button:has-text("取消"), [data-testid="common-close-btn"]');
    await expect(closeButton.first()).toBeVisible();
  });
});
```

**创建 e2e/admin/subscriptions.spec.ts** - 订阅管理页：

```typescript
import { test, expect } from '../fixtures';
import { SubscriptionsPage } from '../pages/admin/subscriptions-page';
import { ADMIN_SUBSCRIPTIONS } from '../testids';

test.describe('订阅管理页面', () => {
  let subscriptionsPage: SubscriptionsPage;

  test.beforeEach(async ({ adminPage }) => {
    subscriptionsPage = new SubscriptionsPage(adminPage);
    await subscriptionsPage.load();
  });

  test('显示订阅列表', async () => {
    await expect(subscriptionsPage.heading).toBeVisible();
    await expect(subscriptionsPage.list).toBeVisible();
  });

  test('状态筛选器可用', async () => {
    await expect(subscriptionsPage.statusFilter).toBeVisible();
  });

  test('服务筛选器可用', async () => {
    await expect(subscriptionsPage.planFilter).toBeVisible();
  });
});
```

关键点：
- 使用 `import { test, expect } from '../fixtures'`
- 使用 `{ adminPage }` fixture
- 无 waitForTimeout，使用 Playwright 自动等待
- 使用 Page Objects 封装
</action>
  <verify>
ls -la e2e/admin/roles.spec.ts
ls -la e2e/admin/subscriptions.spec.ts
grep -n "waitForTimeout" e2e/admin/roles.spec.ts
grep -n "waitForTimeout" e2e/admin/subscriptions.spec.ts
</verify>
  <done>roles.spec.ts 和 subscriptions.spec.ts 创建完成，使用新架构</done>
</task>

<task type="auto">
  <name>Task 2: 创建 brand.spec.ts 和 pricing.spec.ts</name>
  <files>e2e/admin/brand.spec.ts, e2e/admin/pricing.spec.ts</files>
  <action>
**创建 e2e/admin/brand.spec.ts** - 品牌配置页：

```typescript
import { test, expect } from '../fixtures';
import { BrandPage } from '../pages/admin/brand-page';
import { ADMIN_BRAND } from '../testids';

test.describe('品牌配置页面', () => {
  let brandPage: BrandPage;

  test.beforeEach(async ({ adminPage }) => {
    brandPage = new BrandPage(adminPage);
    await brandPage.load();
  });

  test('显示品牌配置表单', async () => {
    await expect(brandPage.heading).toBeVisible();
    await expect(brandPage.nameInput).toBeVisible();
  });

  test('Logo 上传区域存在', async () => {
    await expect(brandPage.logoInput).toBeVisible();
  });

  test('保存按钮存在且可点击', async () => {
    await expect(brandPage.saveButton).toBeVisible();
    await expect(brandPage.saveButton).toBeEnabled();
  });

  test('填写品牌名称后保存', async () => {
    const testBrandName = 'TestBrand_E2E';
    await brandPage.nameInput.fill(testBrandName);
    await brandPage.saveButton.click();
    // 等待保存成功的提示（如果有）
    // 注意：这里不验证实际保存结果，只验证 UI 交互
  });
});
```

**创建 e2e/admin/pricing.spec.ts** - 额度定价页：

```typescript
import { test, expect } from '../fixtures';
import { PricingPage } from '../pages/admin/pricing-page';
import { ADMIN_PRICING } from '../testids';

test.describe('额度定价配置页面', () => {
  let pricingPage: PricingPage;

  test.beforeEach(async ({ adminPage }) => {
    pricingPage = new PricingPage(adminPage);
    await pricingPage.load();
  });

  test('显示价格配置表单', async () => {
    await expect(pricingPage.heading).toBeVisible();
    await expect(pricingPage.priceInput).toBeVisible();
  });

  test('保存按钮存在且可点击', async () => {
    await expect(pricingPage.saveButton).toBeVisible();
    await expect(pricingPage.saveButton).toBeEnabled();
  });

  test('填写价格后保存', async () => {
    const testPrice = '99.99';
    await pricingPage.priceInput.fill(testPrice);
    await pricingPage.saveButton.click();
    // 等待保存成功提示（如果有）
  });
});
```

关键点：
- 使用 `import { test, expect } from '../fixtures'`
- 使用 `{ adminPage }` fixture
- 无 waitForTimeout
- 使用 Page Objects 封装
</action>
  <verify>
ls -la e2e/admin/brand.spec.ts
ls -la e2e/admin/pricing.spec.ts
grep -n "waitForTimeout" e2e/admin/brand.spec.ts
grep -n "waitForTimeout" e2e/admin/pricing.spec.ts
</verify>
  <done>brand.spec.ts 和 pricing.spec.ts 创建完成，使用新架构</done>
</task>

</tasks>

<verification>
- 所有 4 个新 spec 文件使用 `import { test, expect } from '../fixtures'`
- 所有 spec 使用 `{ adminPage }` fixture
- 无 waitForTimeout
- 每个 spec 有至少 3 个测试用例
</verification>

<success_criteria>
- roles.spec.ts、subscriptions.spec.ts、brand.spec.ts、pricing.spec.ts 全部创建完成
- 所有测试使用新架构（fixtures + Page Objects）
- 消除所有 waitForTimeout
- 每个页面至少有 3 个测试用例验证基本功能
</success_criteria>

<output>
After completion, create `.planning/phases/11-admin-web-e2e/11-04-SUMMARY.md`
</output>
