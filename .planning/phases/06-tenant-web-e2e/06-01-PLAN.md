---
phase: 06-tenant-web-e2e
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/dashboard/dashboard.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "用户可以访问 dashboard 页面并看到页面标题"
    - "用户可以看到所有核心指标卡片（公寓数量、房间总数、入住率、活跃租约、租客总数、本月收入、待收账单、逾期账单）"
    - "用户可以点击快捷操作入口跳转到对应页面"
    - "待办提醒区域在水单或水电读数缺失时正常显示"
  artifacts:
    - path: "e2e/dashboard/dashboard.spec.ts"
      provides: "Dashboard E2E 测试"
      min_lines: 100
  key_links:
    - from: "e2e/dashboard/dashboard.spec.ts"
      to: "tenant-web/src/app/dashboard/dashboard-content.tsx"
      via: "data-testid attributes"
      pattern: "dashboard-.*"
---

<objective>
为 tenant-web 创建 Dashboard 页面的完整 E2E 测试覆盖。Dashboard 是用户登录后的首页，需要验证所有核心指标卡片、快捷入口和待办提醒功能正常显示和交互。
</objective>

<execution_context>
@/Users/shen/Desktop/workspace/apartment-ultra/e2e/AGENTS.md
@/Users/shen/Desktop/workspace/apartment-ultra/e2e/helpers/test-data.ts
@/Users/shen/Desktop/workspace/apartment-ultra/e2e/helpers/navigation.ts
@/Users/shen/Desktop/workspace/apartment-ultra/e2e/testids.ts
@/Users/shen/Desktop/workspace/apartment-ultra/tenant-web/src/app/dashboard/dashboard-content.tsx
</execution_context>

<context>
## 测试场景

根据 dashboard-content.tsx 组件结构，需要测试：

1. **页面加载** - dashboard 页面正常显示，数据加载完成
2. **指标卡片显示** - 8 个指标卡片全部可见：
   - 公寓数量 (dashboard-apartment-count)
   - 房间总数 (dashboard-room-count)
   - 入住率 (dashboard-occupancy-rate)
   - 活跃租约 (dashboard-active-leases)
   - 租客总数 (dashboard-tenant-count)
   - 本月收入 (dashboard-monthly-revenue)
   - 待收账单 (dashboard-pending-bills)
   - 逾期账单 (dashboard-overdue-bills)
3. **待办提醒区域** - 当有待办事项时显示（待收账单、逾期账单、未录入水电）
4. **快捷入口跳转** - 待办提醒可以正确跳转到对应页面
5. **无数据状态** - 空组织状态下显示欢迎页

## 测试数据生成器方法

```typescript
import { createTestDataGenerator } from '../helpers/test-data';

// 创建完整测试环境（公寓+租客+租约）
const { apartment, tenant, lease } = await generator.createFullTestEnvironment();

// 创建账单
await generator.createBill(lease.id, { amount: 1500 });
```

## DASHBOARD testids（来自 testids.ts）

```typescript
export const DASHBOARD = {
  HEADING: 'dashboard-heading',
  APARTMENT_COUNT_CARD: 'dashboard-apartment-count',
  ROOM_COUNT_CARD: 'dashboard-room-count',
  OCCUPANCY_RATE_CARD: 'dashboard-occupancy-rate',
  ACTIVE_LEASES_CARD: 'dashboard-active-leases',
  TENANT_COUNT_CARD: 'dashboard-tenant-count',
  MONTHLY_REVENUE_CARD: 'dashboard-monthly-revenue',
  PENDING_BILLS_CARD: 'dashboard-pending-bills',
  OVERDUE_BILLS_CARD: 'dashboard-overdue-bills',
} as const;
```

## 导航辅助函数

```typescript
import { goToDashboard } from '../helpers/navigation';
await goToDashboard(page);
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 dashboard E2E 测试文件</name>
  <files>e2e/dashboard/dashboard.spec.ts</files>
  <read_first>
    - e2e/AGENTS.md
    - e2e/testids.ts (DASHBOARD section)
    - e2e/helpers/test-data.ts
    - e2e/helpers/navigation.ts (goToDashboard function)
    - e2e/fixtures.ts (authenticatedPage fixture)
    - tenant-web/src/app/dashboard/dashboard-content.tsx
    - e2e/leases/crud.spec.ts (参考测试模式)
  </read_first>
  <action>
创建 `e2e/dashboard/dashboard.spec.ts`，包含以下测试用例：

1. **test.describe('Dashboard 页面加载')**
   - `test('成功加载 dashboard 页面', ...)` - 验证页面标题和主要结构
   - `test('所有指标卡片均可见', ...)` - 验证 8 个指标卡片全部显示

2. **test.describe('Dashboard 指标数据')**
   - `test('显示正确的指标数据', ...)` - 创建测试数据后验证指标卡片数值
   - `test('入住率计算正确', ...)` - 验证入住率 = 已入住房间 / 总房间

3. **test.describe('Dashboard 待办提醒')**
   - `test('有待办事项时显示提醒区域', ...)` - 创建待收账单后验证提醒区域显示
   - `test('点击待办提醒跳转到对应页面', ...)` - 点击待收账单跳转 /bills?status=pending

4. **test.describe('Dashboard 无数据状态')**
   - `test('新用户显示欢迎页', ...)` - 验证无组织时的空状态

**关键实现细节：**

```typescript
import { test, expect } from '../fixtures';
import { goToDashboard } from '../helpers/navigation';
import { DASHBOARD } from '../testids';
import { createTestDataGenerator } from '../helpers/test-data';

test.describe('Dashboard 页面加载', () => {
  test.beforeEach(async ({ page }) => {
    await login(page);
    await goToDashboard(page);
  });

  test('成功加载 dashboard 页面', async ({ page }) => {
    await expect(page.locator(`[data-testid="${DASHBOARD.HEADING}"]`)).toBeVisible();
    // 验证页面标题文本为"首页"
    await expect(page.locator(`[data-testid="${DASHBOARD.HEADING}"]`)).toHaveText('首页');
  });

  test('所有指标卡片均可见', async ({ page }) => {
    await expect(page.locator(`[data-testid="${DASHBOARD.APARTMENT_COUNT_CARD}"]`)).toBeVisible();
    await expect(page.locator(`[data-testid="${DASHBOARD.ROOM_COUNT_CARD}"]`)).toBeVisible();
    await expect(page.locator(`[data-testid="${DASHBOARD.OCCUPANCY_RATE_CARD}"]`)).toBeVisible();
    await expect(page.locator(`[data-testid="${DASHBOARD.ACTIVE_LEASES_CARD}"]`)).toBeVisible();
    await expect(page.locator(`[data-testid="${DASHBOARD.TENANT_COUNT_CARD}"]`)).toBeVisible();
    await expect(page.locator(`[data-testid="${DASHBOARD.MONTHLY_REVENUE_CARD}"]`)).toBeVisible();
    await expect(page.locator(`[data-testid="${DASHBOARD.PENDING_BILLS_CARD}"]`)).toBeVisible();
    await expect(page.locator(`[data-testid="${DASHBOARD.OVERDUE_BILLS_CARD}"]`)).toBeVisible();
  });
});

test.describe('Dashboard 指标数据', () => {
  test('显示正确的指标数据', async ({ page, request }) => {
    await login(page);

    const generator = await createTestDataGenerator(request);
    try {
      // 创建完整测试环境
      const { apartment, tenant, lease } = await generator.createFullTestEnvironment();

      // 创建账单
      await generator.createBill(lease.id, { amount: 2000 });

      // 导航到 dashboard
      await goToDashboard(page);

      // 等待数据加载
      await page.waitForTimeout(1000);

      // 验证公寓数量 >= 1
      const apartmentCard = page.locator(`[data-testid="${DASHBOARD.APARTMENT_COUNT_CARD}"]`);
      await expect(apartmentCard).toBeVisible();
      const apartmentValue = await apartmentCard.locator('.text-2xl').textContent();
      expect(parseInt(apartmentValue || '0')).toBeGreaterThanOrEqual(1);

      // 验证活跃租约 >= 1
      const leaseCard = page.locator(`[data-testid="${DASHBOARD.ACTIVE_LEASES_CARD}"]`);
      await expect(leaseCard).toBeVisible();
    } finally {
      await generator.cleanup();
    }
  });
});

test.describe('Dashboard 待办提醒', () => {
  test('有待办事项时显示提醒区域', async ({ page, request }) => {
    await login(page);

    const generator = await createTestDataGenerator(request);
    try {
      const { lease } = await generator.createFullTestEnvironment();
      await generator.createBill(lease.id, { amount: 1500 });

      await goToDashboard(page);
      await page.waitForTimeout(1000);

      // 验证待办提醒区域或空状态都正常（取决于是否有待办）
      const remindersSection = page.locator('text=待办提醒');
      if (await remindersSection.isVisible()) {
        // 有待办时验证待办项
        await expect(page.locator('text=待收账单')).toBeVisible();
      }
    } finally {
      await generator.cleanup();
    }
  });

  test('点击待办提醒跳转到对应页面', async ({ page, request }) => {
    await login(page);

    const generator = await createTestDataGenerator(request);
    try {
      const { lease } = await generator.createFullTestEnvironment();
      await generator.createBill(lease.id, { amount: 1500 });

      await goToDashboard(page);
      await page.waitForTimeout(1000);

      // 查找并点击待收账单入口
      const pendingBillLink = page.locator('a[href="/bills?status=pending"]');
      if (await pendingBillLink.isVisible()) {
        await pendingBillLink.click();
        await page.waitForURL('**/bills**');
        // 验证跳转成功
        await expect(page.locator('[data-testid="bills-heading"]')).toBeVisible();
      }
    } finally {
      await generator.cleanup();
    }
  });
});
```

**注意事项：**
- 使用 `login(page)` 从 `../helpers/auth` 登录
- 使用 `goToDashboard(page)` 从 `../helpers/navigation` 导航
- 使用 `createTestDataGenerator(request)` 创建测试数据
- 每个测试用 `try/finally` 确保 `generator.cleanup()` 调用
- 避免 `waitForTimeout()`，使用 `expect().toBeVisible()` 自动等待
- 禁止 `test.skip()`
  </action>
  <verify>
    <automated>
      cd /Users/shen/Desktop/workspace/apartment-ultra && npx playwright test e2e/dashboard/dashboard.spec.ts --reporter=list 2>&1 | head -50
    </automated>
  </verify>
  <done>Dashboard E2E 测试文件创建完成，包含页面加载、指标显示、待办提醒、快捷入口跳转等测试场景</done>
</task>

</tasks>

<verification>
- 测试文件 `e2e/dashboard/dashboard.spec.ts` 存在
- 文件包含至少 5 个 test.describe 块，覆盖核心场景
- 使用 `DASHBOARD` testids 常量
- 使用 `login()` 和 `goToDashboard()` 辅助函数
- 使用 `createTestDataGenerator()` 创建独立测试数据
- 每个测试包含 `try/finally` 清理逻辑
</verification>

<success_criteria>
- Dashboard E2E 测试文件已创建
- 测试用例覆盖：页面加载验证、指标卡片显示、指标数据正确性、待办提醒显示、快捷入口跳转
- 所有测试使用 `data-testid` 属性定位元素
- 每个测试独立数据，使用 TestDataGenerator 创建和清理
- CI 配置准备好运行 E2E 测试（`pnpm test:e2e`）
</success_criteria>

<output>
After completion, create `.planning/phases/06-tenant-web-e2e/06-01-SUMMARY.md`
</output>
