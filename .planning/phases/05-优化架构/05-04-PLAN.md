---
phase: 05-优化架构
plan: '04'
type: execute
wave: 4
depends_on:
  - '05-01'
files_modified:
  - api/src/utils/logger.ts
  - api/src/index.ts
  - api/src/services/bill.service.ts
  - api/src/services/lease.service.ts
  - api/src/services/billGeneration.ts
  - api/src/services/wechatPayNative.ts
  - api/src/scheduler/index.ts
  - api/src/scheduler/notificationChecks.ts
  - api/src/scheduler/monthlyBills.ts
  - api/src/utils/audit.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "All services and scheduler use pino structured logger"
    - "No console.log or console.error in production code"
    - "Logs include request ID when available"
  artifacts:
    - path: api/src/utils/logger.ts
      provides: Pino logger singleton with request ID support
      exports: logger, setRequestId, getRequestId, child
    - path: api/src/index.ts
      provides: Logger initialization at startup
      initializes: pino with JSON format
    - path: api/src/services/*.service.ts
      provides: All services using logger instead of console
      replaces: console.log/error with logger.info/error
    - path: api/src/scheduler/*.ts
      provides: Scheduler using logger instead of console
      replaces: console.log/error with logger.info/error
  key_links:
    - from: api/src/index.ts
      to: api/src/utils/logger.ts
      via: import logger
    - from: services
      to: api/src/utils/logger.ts
      via: import { logger } from '../utils/logger.js'
---

<objective>
用 pino 结构化日志替换所有 services 和 scheduler 中的 console.log/error。Logger 在 api/src/index.ts 初始化，services 和 scheduler 导入使用。

Purpose: 生产环境可观测性改进，JSON 格式日志便于日志聚合和分析。
Output: 所有 services 和 scheduler 使用 logger，console.log/error 从生产代码中移除。
</objective>

<context>
@api/src/index.ts
@api/src/services/bill.service.ts
@api/src/services/lease.service.ts
@api/src/scheduler/index.ts
@api/src/scheduler/notificationChecks.ts
@api/src/utils/audit.ts

<interfaces>
logger.ts 导出:
```typescript
import pino from 'pino';

export const logger = pino({
  level: process.env.LOG_LEVEL ?? 'info',
  formatters: { level: (label) => ({ level: label }) },
  timestamp: pino.stdTimeFunctions.isoTime,
});

export function setRequestId(id: string): void;
export function getRequestId(): string | undefined;
export function child(bindings: Record<string, unknown>): pino.Logger;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 api/src/utils/logger.ts</name>
  <files>api/src/utils/logger.ts</files>
  <read_first>
    - (无 - 新文件)
  </read_first>
  <action>
创建 api/src/utils/logger.ts:

```typescript
import pino from 'pino';

/** 全局 logger 实例 */
export const logger = pino({
  level: process.env.LOG_LEVEL ?? 'info',
  formatters: {
    level: (label) => ({ level: label }),
  },
  timestamp: pino.stdTimeFunctions.isoTime,
  base: {
    service: 'apartment-ultra-api',
  },
});

/** 当前请求的 request ID（用于关联日志） */
let currentRequestId: string | undefined;

export function setRequestId(id: string): void {
  currentRequestId = id;
}

export function getRequestId(): string | undefined {
  return currentRequestId;
}

/**
 * 创建子 logger，带有额外的绑定信息
 * 例如: logger.child({ operation: 'bill_generation' })
 */
export function child(bindings: Record<string, unknown>): pino.Logger {
  return logger.child(bindings);
}
```

注意: 需要安装 pino 相关包（如果尚未安装）:
```bash
cd api && pnpm add pino pino-http
```
  </action>
  <verify>
    <automated>ls -la api/src/utils/logger.ts</automated>
  </verify>
  <done>api/src/utils/logger.ts 创建完成，导出 logger, setRequestId, getRequestId, child</done>
</task>

<task type="auto">
  <name>Task 2: 在 api/src/index.ts 初始化 logger</name>
  <files>api/src/index.ts</files>
  <read_first>
    - api/src/index.ts (当前有 console.error 启动失败消息)
  </read_first>
  <action>
更新 api/src/index.ts:

1. 添加 import（在文件顶部）:
```typescript
import { logger } from './utils/logger.js';
```

2. 替换启动失败消息:
```typescript
// 替换 console.error('[启动失败] 无法连接数据库，退出...');
logger.error({ err: 'database_connection_failed' }, '无法连接数据库，退出...');
```

```typescript
// 替换 console.error('[启动失败] 数据库 schema 不完整...');
logger.error({ err: 'database_schema_invalid' }, '数据库 schema 不完整，请运行 prisma db push');
```

3. 替换启动成功消息:
```typescript
// 替换 console.log(`${config.appName} listening on port ${port}`);
logger.info({ port, service: config.appName }, '服务启动');
```

注意: 确保 logger 初始化在其他 import 之后，但在实际启动流程中，pino 是同步的，可以在 express app 创建之后才初始化 logger。
  </action>
  <verify>
    <automated>grep -c "from './utils/logger.js'" api/src/index.ts && grep -c "logger.error\|logger.info" api/src/index.ts</automated>
  </verify>
  <done>api/src/index.ts 使用 logger 替代 console</done>
</task>

<task type="auto">
  <name>Task 3: 替换 services 中的 console.log/error</name>
  <files>api/src/services/bill.service.ts,api/src/services/lease.service.ts,api/src/services/billGeneration.ts,api/src/services/wechatPayNative.ts</files>
  <read_first>
    - api/src/services/bill.service.ts (有 console.error)
    - api/src/services/lease.service.ts (有 console.error)
    - api/src/services/billGeneration.ts (有 console.error)
    - api/src/services/wechatPayNative.ts (有 console.error)
  </read_first>
  <action>
每个 service 文件:

1. 添加 import:
```typescript
import { logger } from '../utils/logger.js';
```

2. 替换 console.error 为 logger.error:
```typescript
// bill.service.ts:144
// 替换: console.error('[bill.service] failed to send tenant bill_generated sms:', error);
logger.error({ err: error, orgId: /* relevant context */ }, 'failed to send tenant bill_generated sms');
```

```typescript
// lease.service.ts:184, 226
// 替换: console.error('Failed to create tenant_move_in notification:', e);
logger.error({ err: e, leaseId: /* relevant context */ }, 'Failed to create tenant_move_in notification');
```

```typescript
// billGeneration.ts:181
// 替换: console.error('[billGeneration] failed to send tenant bill_generated sms:', error);
logger.error({ err: error, orgId: /* context */ }, 'failed to send tenant bill_generated sms');
```

```typescript
// wechatPayNative.ts:113
// 替换: console.error('WeChat Pay Native order failed:', res.status, errText);
logger.error({ status: res.status, errText }, 'WeChat Pay Native order failed');
```

注意: logger.error 第二个参数是 message string，第一个参数是对象包含上下文变量。
  </action>
  <verify>
    <automated>grep -c "console.error" api/src/services/bill.service.ts api/src/services/lease.service.ts api/src/services/billGeneration.ts api/src/services/wechatPayNative.ts</automated>
  </verify>
  <done>所有 services 中的 console.error 替换为 logger.error</done>
</task>

<task type="auto">
  <name>Task 4: 替换 scheduler 中的 console.log/error</name>
  <files>api/src/scheduler/index.ts,api/src/scheduler/notificationChecks.ts,api/src/scheduler/monthlyBills.ts</files>
  <read_first>
    - api/src/scheduler/index.ts (有 console.log/error)
    - api/src/scheduler/notificationChecks.ts (有 console.error)
    - api/src/scheduler/monthlyBills.ts (有 console.error)
  </read_first>
  <action>
每个 scheduler 文件:

1. 添加 import:
```typescript
import { logger } from '../utils/logger.js';
```

2. 替换:
```typescript
// scheduler/index.ts
// 替换: console.log('Monthly bill generation completed:', stats);
logger.info({ stats }, 'Monthly bill generation completed');
// 替换: console.error('Monthly bill generation failed:', e);
logger.error({ err: e }, 'Monthly bill generation failed');
// 替换: console.log('Scheduler started...');
logger.info('Scheduler started');
```

```typescript
// scheduler/notificationChecks.ts:102, 172
// 替换: console.error('[notificationChecks] failed to send tenant bill_overdue sms:', error);
logger.error({ err: error, /* context */ }, 'failed to send tenant bill_overdue sms');
```

```typescript
// scheduler/monthlyBills.ts:31
// 替换: console.error('Monthly bill generation error for org', org.id, e);
logger.error({ err: e, orgId: org.id }, 'Monthly bill generation error');
```
  </action>
  <verify>
    <automated>grep -c "console.log\|console.error" api/src/scheduler/index.ts api/src/scheduler/notificationChecks.ts api/src/scheduler/monthlyBills.ts</automated>
  </verify>
  <done>所有 scheduler 中的 console.log/error 替换为 logger</done>
</task>

<task type="auto">
  <name>Task 5: 替换 audit.ts 中的 console.log</name>
  <files>api/src/utils/audit.ts</files>
  <read_first>
    - api/src/utils/audit.ts (有 console.log)
  </read_first>
  <action>
更新 api/src/utils/audit.ts:

1. 添加 import:
```typescript
import { logger } from './logger.js';
```

2. 替换:
```typescript
// 替换: console.log('[AUDIT]', JSON.stringify(logData));
logger.info({ ...logData, type: 'audit' }, 'AUDIT');
```

注意: audit utility 特殊，audit logs 输出到 stdout（console.log），但改用 logger 后默认也是输出到 stdout，只是格式变成 JSON，更利于日志聚合。
  </action>
  <verify>
    <automated>grep -c "console.log" api/src/utils/audit.ts</automated>
  </verify>
  <done>audit.ts 中的 console.log 替换为 logger.info</done>
</task>

</tasks>

<verification>
- [ ] `ls api/src/utils/logger.ts` 文件存在
- [ ] `grep -c "console.error\|console.log" api/src/services/*.service.ts` 全为 0
- [ ] `grep -c "console.error\|console.log" api/src/scheduler/*.ts` 全为 0
- [ ] `grep -c "console.log" api/src/utils/audit.ts` 为 0
- [ ] `grep "logger" api/src/index.ts` 有输出
- [ ] `pnpm --filter apartment-ultra-api exec tsc --noEmit` 无错误
</verification>

<success_criteria>
1. api/src/utils/logger.ts 创建完成，导出 logger
2. api/src/index.ts 使用 logger 替代 console
3. 所有 services 中的 console.error 替换为 logger.error
4. 所有 scheduler 中的 console.log/error 替换为 logger
5. api/src/utils/audit.ts 中的 console.log 替换为 logger
6. `pnpm type-check` 通过
</success_criteria>

<output>
After completion, create `.planning/phases/05-优化架构/05-04-SUMMARY.md`
</output>
