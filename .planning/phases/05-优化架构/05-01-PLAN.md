---
phase: 05-优化架构
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api-contract/src/permissions.ts
  - api/src/constants/permissionDefaults.ts
  - tenant-web/src/lib/constants/admin-permissions.ts
  - tenant-web/src/lib/permission-access.ts
  - api/src/services/permission.service.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Permission codes are defined in api-contract once, no duplication"
    - "Backend and frontend use the same permission code constants"
    - "RESOURCES and ACTIONS arrays are shared across all codebases"
  artifacts:
    - path: packages/api-contract/src/permissions.ts
      provides: Single source of truth for permission codes
      exports: RESOURCES, ACTIONS, RESOURCE_NAMES, ACTION_NAMES, toPermissionCodes function
    - path: api/src/constants/permissionDefaults.ts
      provides: Backend permission constants
      imports_from: api-contract
    - path: tenant-web/src/lib/constants/admin-permissions.ts
      provides: Frontend permission constants
      imports_from: api-contract
    - path: tenant-web/src/lib/permission-access.ts
      provides: Permission access logic
      imports_from: api-contract
    - path: api/src/services/permission.service.ts
      provides: Permission enforcement
      imports_from: api-contract
  key_links:
    - from: api-contract/src/permissions.ts
      to: api/src/constants/permissionDefaults.ts
      via: import RESOURCES, ACTIONS
    - from: api-contract/src/permissions.ts
      to: tenant-web/src/lib/constants/admin-permissions.ts
      via: import RESOURCES, ACTIONS
---

<objective>
建立权限系统的单一数据源 (Single Source of Truth)。将 RESOURCES、ACTIONS 和所有权限码定义到 api-contract/src/permissions.ts，后端和前端都从 api-contract 导入，不再各自定义。

Purpose: 消除前后端权限码硬编码导致的漂移 (drift)，确保权限码始终一致。
Output: api-contract/src/permissions.ts 包含所有共享权限常量，4个文件更新为从 api-contract 导入。
</objective>

<context>
@packages/api-contract/src/index.ts
@packages/api-contract/src/common.ts
@packages/api-contract/src/permissions.ts
@api/src/constants/permissionDefaults.ts
@tenant-web/src/lib/constants/admin-permissions.ts
@tenant-web/src/lib/permission-access.ts
@api/src/services/permission.service.ts

<interfaces>
packages/api-contract/src/permissions.ts 需要导出:
```typescript
export const RESOURCES = ['apartment', 'room', 'tenant', 'lease', 'bill', 'utility', 'member', 'settings', 'report'] as const;
export const ACTIONS = ['view', 'create', 'edit', 'delete', 'export'] as const;
export const RESOURCE_NAMES: Record<typeof RESOURCES[number], string> = { ... };
export const ACTION_NAMES: Record<typeof ACTIONS[number], string> = { ... };
export function toPermissionCodes(perms: Array<{ resource: typeof RESOURCES[number]; action: typeof ACTIONS[number] }>): string[]
```

注意: api-contract/src/permissions.ts 已存在但只包含 TypeScript 接口，不包含实际权限码常量。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 api-contract permissions.ts 常量导出</name>
  <files>packages/api-contract/src/permissions.ts</files>
  <read_first>
    - packages/api-contract/src/permissions.ts (当前只导出接口)
    - api/src/constants/permissionDefaults.ts (参考现有 RESOURCES, ACTIONS, RESOURCE_NAMES, ACTION_NAMES 定义)
  </read_first>
  <action>
在 packages/api-contract/src/permissions.ts 中添加以下导出（放在文件顶部，接口定义之前）:

```typescript
// ===== 权限码常量（共享给所有应用） =====

/** 资源类型 */
export const RESOURCES = ['apartment', 'room', 'tenant', 'lease', 'bill', 'utility', 'member', 'settings', 'report'] as const;
export type Resource = typeof RESOURCES[number];

/** 操作类型 */
export const ACTIONS = ['view', 'create', 'edit', 'delete', 'export'] as const;
export type Action = typeof ACTIONS[number];

/** 资源中文名 */
export const RESOURCE_NAMES: Record<Resource, string> = {
  apartment: '公寓管理',
  room: '房间管理',
  tenant: '租客管理',
  lease: '租约管理',
  bill: '账单管理',
  utility: '水电管理',
  member: '成员管理',
  settings: '系统设置',
  report: '报表分析',
};

/** 操作中文名 */
export const ACTION_NAMES: Record<Action, string> = {
  view: '查看',
  create: '创建',
  edit: '编辑',
  delete: '删除',
  export: '导出',
};

/**
 * 将 (resource, action) 列表转为权限码列表
 * 例如: [{ resource: 'apartment', action: 'view' }] => ['apartment:view']
 */
export function toPermissionCodes(
  perms: Array<{ resource: Resource; action: Action }>
): string[] {
  return perms.map((p) => `${p.resource}:${p.action}`);
}
```

同时更新 packages/api-contract/src/index.ts 添加导出:
```typescript
export { RESOURCES, ACTIONS, RESOURCE_NAMES, ACTION_NAMES, toPermissionCodes, type Resource, type Action } from './permissions.js';
```
  </action>
  <verify>
    <automated>cd /Users/shen/workspace/apartment-ultra && grep -c "export const RESOURCES" packages/api-contract/src/permissions.ts && grep -c "export const ACTIONS" packages/api-contract/src/permissions.ts && grep -c "export function toPermissionCodes" packages/api-contract/src/permissions.ts</automated>
  </verify>
  <done>api-contract/src/permissions.ts 导出 RESOURCES, ACTIONS, RESOURCE_NAMES, ACTION_NAMES, toPermissionCodes</done>
</task>

<task type="auto">
  <name>Task 2: 更新 api/src/constants/permissionDefaults.ts 导入 api-contract</name>
  <files>api/src/constants/permissionDefaults.ts</files>
  <read_first>
    - api/src/constants/permissionDefaults.ts (当前从本地定义 RESOURCES, ACTIONS)
  </read_first>
  <action>
更新 api/src/constants/permissionDefaults.ts:

1. 添加 import 从 api-contract:
```typescript
import { RESOURCES, ACTIONS, type Resource, type Action } from '@apartment-ultra/api-contract';
```

2. 删除本地定义的 RESOURCES 和 ACTIONS 常量（但保留 RESOURCE_NAMES, ACTION_NAMES 因为这些是 UI 标签，按 CONTEXT.md 应该在 per-app）

3. 保留 toPermissionCodes 函数（从 api-contract 导入）

4. 保留 SYSTEM_ROLES, ORG_MEMBER_ROLES, DEFAULT_SYSTEM_ROLE_PERMISSIONS, DEFAULT_ORG_ROLE_PERMISSIONS（这些是角色定义，不是权限码）

5. 保留 type OrgMemberRole = (typeof ORG_MEMBER_ROLES)[number] 等类型定义

注意: RESOURCE_NAMES 和 ACTION_NAMES 仍然保留在 permissionDefaults.ts 中（per-app UI concerns，不共享）
  </action>
  <verify>
    <automated>grep -c "from '@apartment-ultra/api-contract'" api/src/constants/permissionDefaults.ts</automated>
  </verify>
  <done>permissionDefaults.ts 从 api-contract 导入 RESOURCES, ACTIONS，保留本地 RESOURCE_NAMES (UI 标签)</done>
</task>

<task type="auto">
  <name>Task 3: 更新 tenant-web 权限相关文件导入 api-contract</name>
  <files>tenant-web/src/lib/constants/admin-permissions.ts,tenant-web/src/lib/permission-access.ts</files>
  <read_first>
    - tenant-web/src/lib/constants/admin-permissions.ts (ADMIN_PERMISSION_OPTIONS 定义)
    - tenant-web/src/lib/permission-access.ts (AccessRule, canAccessRule 定义)
  </read_first>
  <action>
**admin-permissions.ts 更新**:
- 这是运营后台权限 (admin:*)，与业务端权限 (apartment:view 等) 是不同系统
- 不需要从 api-contract 导入业务权限码
- 但可以添加 import 确认体系结构清晰（留作后续扩展）

**permission-access.ts 更新**:
- 添加 import 从 api-contract:
```typescript
import { RESOURCES, ACTIONS } from '@apartment-ultra/api-contract';
```
- AccessRule 接口中的 permission 类型已经是 string，不需要改
- 可以添加注释说明 RESOURCE_NAMES 从 permissionDefaults.ts 导入
  </action>
  <verify>
    <automated>grep -c "from '@apartment-ultra/api-contract'" tenant-web/src/lib/constants/admin-permissions.ts tenant-web/src/lib/permission-access.ts</automated>
  </verify>
  <done>tenant-web 权限文件导入 api-contract（admin 权限码保持不变）</done>
</task>

<task type="auto">
  <name>Task 4: 更新 api/src/services/permission.service.ts 导入 api-contract</name>
  <files>api/src/services/permission.service.ts</files>
  <read_first>
    - api/src/services/permission.service.ts (当前从 permissionDefaults 导入 toPermissionCodes)
  </read_first>
  <action>
更新 import 语句:
```typescript
import {
  ORG_MEMBER_ROLES,
  DEFAULT_ORG_ROLE_PERMISSIONS,
  toPermissionCodes,
  type SystemRole,
  type OrgMemberRole,
} from '../constants/permissionDefaults.js';
import { RESOURCES, ACTIONS } from '@apartment-ultra/api-contract';
```

由于 permissionDefaults.ts 现在从 api-contract 导入 RESOURCES, ACTIONS，permission.service.ts 可以直接从 api-contract 导入这两个常量（不再经过 permissionDefaults）。

如果 toPermissionCodes 已改从 api-contract 导出，permission.service.ts 中的导入路径仍然用 permissionDefaults.js（因为 permissionDefaults.js 重新导出了它）。
  </action>
  <verify>
    <automated>grep -c "from '@apartment-ultra/api-contract'" api/src/services/permission.service.ts</automated>
  </verify>
  <done>permission.service.ts 从 api-contract 导入 RESOURCES, ACTIONS</done>
</task>

</tasks>

<verification>
- [ ] `grep -c "RESOURCES" packages/api-contract/src/permissions.ts` 返回非零
- [ ] `grep -c "ACTIONS" packages/api-contract/src/permissions.ts` 返回非零
- [ ] `grep "from '@apartment-ultra/api-contract'" api/src/constants/permissionDefaults.ts` 有输出
- [ ] `grep "RESOURCE_NAMES" api/src/constants/permissionDefaults.ts` 仍有 RESOURCE_NAMES 定义（UI 标签保留在 per-app）
- [ ] `pnpm --filter apartment-ultra-api exec tsc --noEmit` 无错误
</verification>

<success_criteria>
1. api-contract/src/permissions.ts 导出 RESOURCES, ACTIONS, RESOURCE_NAMES, ACTION_NAMES, toPermissionCodes
2. api/src/constants/permissionDefaults.ts 从 api-contract 导入 RESOURCES, ACTIONS
3. tenant-web 权限文件从 api-contract 导入（admin 权限码保持不变）
4. api/src/services/permission.service.ts 从 api-contract 导入 RESOURCES, ACTIONS
5. `pnpm type-check` 通过
</success_criteria>

<output>
After completion, create `.planning/phases/05-优化架构/05-01-SUMMARY.md`
</output>
