---
phase: 08-运营数据看板
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - admin-web/src/app/page.tsx
  - admin-web/src/app/dashboard-content.tsx
  - admin-web/src/components/dashboard/stat-card.tsx
  - admin-web/src/components/dashboard/chart-card.tsx
  - admin-web/src/components/dashboard/income-chart.tsx
  - admin-web/src/components/dashboard/occupancy-chart.tsx
  - admin-web/src/components/dashboard/year-filter.tsx
  - admin-web/src/components/dashboard/refresh-button.tsx
  - admin-web/src/components/dashboard/skeleton.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "Admin can see 6 stat cards: 公寓数, 房间数, 入住率, 本月收入, 待缴账单, 逾期账单"
    - "Admin can see income trend chart (bar + line combo)"
    - "Admin can see occupancy rate chart (area chart with 80% reference line)"
    - "Admin can select year to filter chart data"
    - "Admin can refresh all dashboard data"
  artifacts:
    - path: "admin-web/src/app/page.tsx"
      provides: "Dashboard page entry replacing existing stats page"
      min_lines: 20
    - path: "admin-web/src/components/dashboard/stat-card.tsx"
      provides: "Reusable stat card component matching UI-SPEC"
      min_lines: 25
    - path: "admin-web/src/components/dashboard/chart-card.tsx"
      provides: "Chart container wrapper"
      min_lines: 15
    - path: "admin-web/src/components/dashboard/income-chart.tsx"
      provides: "Income trend ComposedChart with bars + line"
      min_lines: 60
    - path: "admin-web/src/components/dashboard/occupancy-chart.tsx"
      provides: "Occupancy AreaChart with reference line"
      min_lines: 50
    - path: "admin-web/src/components/dashboard/year-filter.tsx"
      provides: "Year selection dropdown"
      min_lines: 20
    - path: "admin-web/src/components/dashboard/refresh-button.tsx"
      provides: "Refresh button with loading state"
      min_lines: 15
  key_links:
    - from: "admin-web/src/app/page.tsx"
      to: "admin-web/src/components/dashboard/*"
      via: "imports"
      pattern: "from '@/components/dashboard"
    - from: "admin-web/src/app/page.tsx"
      to: "admin-web/src/lib/api/admin-client.ts"
      via: "getStats"
      pattern: "adminApiEndpoints\\.getStats"
---

<objective>
Build the operations dashboard (平台概览) for admin-web. This replaces the existing basic stats page with a full-featured dashboard showing 6 stat cards, income trend chart, occupancy chart, year filter, and refresh button.
</objective>

<execution_context>
@/Users/shen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@admin-web/src/app/page.tsx
@admin-web/src/lib/api/admin-client.ts
@tenant-web/src/app/dashboard/dashboard-content.tsx
@packages/api-contract/src/reports.ts
@packages/api-contract/src/admin.ts

# Key Interface Contracts

From admin-web/src/lib/api/admin-client.ts:
```typescript
// getStats returns AdminPlatformStats (no orgId required - admin-level aggregate)
getStats: () => adminApi.get<AdminPlatformStats>('/admin/stats')

interface AdminPlatformStats {
  organizations_count: number;
  users_count: number;
  apartments_count: number;
  rooms_count: number;
  active_subscriptions_count: number;
}
```

From packages/api-contract/src/reports.ts:
```typescript
interface DashboardOverview {
  total_apartments: number;
  total_rooms: number;
  occupied_rooms: number;
  available_rooms: number;
  total_tenants: number;
  active_leases: number;
  occupancy_rate: number;
  monthly_revenue: number;
  pending_bills: number;
  overdue_bills: number;
}

interface IncomeReport {
  period: string;
  total_rent: number;
  total_water: number;
  total_electricity: number;
  total_other: number;
  total_amount: number;
  collected_amount: number;
  collection_rate: number;
}

interface OccupancyReport {
  period: string;
  total_rooms: number;
  occupied_rooms: number;
  vacant_rooms: number;
  occupancy_rate: number;
}
```

From 08-UI-SPEC.md (copywriting contract):
| Element | Copy |
|---------|------|
| Page Title | 平台概览 |
| Stat Card — 公寓数 | 公寓数 |
| Stat Card — 房间数 | 房间数 |
| Stat Card — 入住率 | 入住率 |
| Stat Card — 本月收入 | 本月收入 |
| Stat Card — 待缴账单 | 待缴账单 |
| Stat Card — 逾期账单 | 逾期账单 |
| Chart Title — Income | 收入趋势 |
| Chart Title — Occupancy | 入住率分析 |
| Refresh Button | 刷新数据 |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard component structure</name>
  <files>admin-web/src/app/dashboard-content.tsx, admin-web/src/components/dashboard/stat-card.tsx, admin-web/src/components/dashboard/chart-card.tsx, admin-web/src/components/dashboard/skeleton.tsx</files>
  <read_first>
    - admin-web/src/app/page.tsx (current page to replace)
    - tenant-web/src/app/dashboard/dashboard-content.tsx (reference pattern)
    - packages/shared-ui/src/components/ui/card.tsx (Card component)
    - packages/shared-ui/src/components/ui/skeleton.tsx (Skeleton component)
  </read_first>
  <action>
    Create the following files with exact implementations:

    1. **admin-web/src/components/dashboard/stat-card.tsx**
       - Uses Card, CardHeader, CardContent from shared-ui
       - Props: title (string), value (number | string), isPercentage (boolean), isCurrency (boolean)
       - If isPercentage: show value with 1 decimal + "%"
       - If isCurrency: show "¥" + value.toLocaleString() with 2 decimals
       - Otherwise: show value.toLocaleString()
       - Title: 14px medium muted-foreground
       - Value: 28px bold (text-2xl font-bold)
       - Hover: shadow-sm
       - data-testid="stat-card-{title-slug}"

    2. **admin-web/src/components/dashboard/chart-card.tsx**
       - Wrapper around Card with CardHeader + CardContent
       - Props: title (string), children (React.ReactNode)
       - Title: 20px semibold (text-xl font-semibold)
       - Content: p-6, children fills here
       - Chart height: h-[320px]

    3. **admin-web/src/components/dashboard/skeleton.tsx**
       - StatCardsSkeleton: 6 Skeleton cards matching stat card dimensions
       - ChartSkeleton: 2 Skeleton cards matching chart card dimensions

    4. **admin-web/src/app/dashboard-content.tsx**
       - 'use client' component
       - Uses TanStack Query useQuery for data
       - QueryKey: ['admin-dashboard', selectedYear]
       - Imports and composes: StatCard, ChartCard, IncomeChart, OccupancyChart, YearFilter, RefreshButton, Skeletons
       - Layout matches UI-SPEC:
         - Page title "平台概览" top-left
         - Refresh button top-right
         - Stat cards row: grid-cols-2 lg:grid-cols-3 xl:grid-cols-6 (6 cards)
         - Year filter above charts
         - Charts row: grid-cols-1 lg:grid-cols-5 (income 3 cols, occupancy 2 cols)
       - Uses existing adminApiEndpoints.getStats() for initial data
       - Stat cards values: apartments_count, rooms_count, occupancy_rate%, monthly_revenue, pending_bills, overdue_bills
       - For income/occupancy data: use mock data arrays initially (IncomeReport[], OccupancyReport[])
       - Handle loading: show StatCardsSkeleton while stats loading
       - Handle error: show error message with retry button

    5. **admin-web/src/app/page.tsx** (MODIFY)
       - Replace current content with DashboardContent import
       - Keep existing auth check, MainLayout wrapping
       - Remove old stats content
  </action>
  <verify>
    <automated>grep -l "StatCard\|ChartCard\|DashboardContent" admin-web/src/app/dashboard-content.tsx admin-web/src/components/dashboard/*.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - admin-web/src/components/dashboard/stat-card.tsx exists with StatCard export
    - admin-web/src/components/dashboard/chart-card.tsx exists with ChartCard export
    - admin-web/src/components/dashboard/skeleton.tsx exists with StatCardsSkeleton and ChartSkeleton exports
    - admin-web/src/app/dashboard-content.tsx exists and imports all dashboard components
    - admin-web/src/app/page.tsx imports DashboardContent instead of old stats
    - StatCard renders value formatted per type (percentage/currency/number)
    - StatCard hover state has shadow-sm
    - Dashboard layout matches UI-SPEC grid spec
  </acceptance_criteria>
  <done>Dashboard component structure created with StatCard, ChartCard, skeleton, and page layout</done>
</task>

<task type="auto">
  <name>Task 2: Create chart components</name>
  <files>admin-web/src/components/dashboard/income-chart.tsx, admin-web/src/components/dashboard/occupancy-chart.tsx</files>
  <read_first>
    - packages/api-contract/src/reports.ts (IncomeReport, OccupancyReport types)
    - admin-web/package.json (confirm recharts is installed)
  </read_first>
  <action>
    Create the following chart components using recharts:

    1. **admin-web/src/components/dashboard/income-chart.tsx**
       - Import: ComposedChart, Bar, Line, XAxis, YAxis, Tooltip, ResponsiveContainer, ReferenceLine from recharts
       - Props: data: IncomeReport[], isLoading: boolean
       - If isLoading: return null (skeleton shown by parent)
       - ComposedChart with bars + line:
         - Bars: collected_amount (chart-1 fill), uncollected (chart-3 fill)
         - Line: collection_rate (chart-2 stroke)
       - XAxis: dataKey="period", tickFormatter to show month only
       - YAxis (left): tickFormatter with "¥" prefix for amounts
       - YAxis (right): domain [0, 100] for collection_rate with "%" suffix
       - Tooltip: custom showing month + amounts + rate
       - Legend: showing 收租 (collected), 未收 (uncollected), 收租率 (rate)
       - Colors: chart-1, chart-2, chart-3 from tailwind config (use CSS vars or hex)
       - ResponsiveContainer width="100%" height={280}

    2. **admin-web/src/components/dashboard/occupancy-chart.tsx**
       - Import: AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer, ReferenceLine from recharts
       - Props: data: OccupancyReport[], isLoading: boolean
       - If isLoading: return null
       - AreaChart with occupancy_rate (chart-2 fill with opacity 0.3, stroke solid)
       - XAxis: dataKey="period", tickFormatter for month
       - YAxis: domain [0, 100], tickFormatter with "%" suffix
       - ReferenceLine at y={80} with dashed stroke, label "80%预警线"
       - Tooltip: custom showing month + occupancy_rate% + room counts
       - ResponsiveContainer width="100%" height={280}
  </action>
  <verify>
    <automated>grep -l "ComposedChart\|AreaChart" admin-web/src/components/dashboard/income-chart.tsx admin-web/src/components/dashboard/occupancy-chart.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - admin-web/src/components/dashboard/income-chart.tsx uses ComposedChart with Bar + Line
    - admin-web/src/components/dashboard/income-chart.tsx has collected_amount bar, uncollected bar, collection_rate line
    - admin-web/src/components/dashboard/occupancy-chart.tsx uses AreaChart
    - admin-web/src/components/dashboard/occupancy-chart.tsx has 80% ReferenceLine
    - Both charts use ResponsiveContainer
    - Both charts accept data and isLoading props
  </acceptance_criteria>
  <done>Chart components created with IncomeChart (ComposedChart) and OccupancyChart (AreaChart)</done>
</task>

<task type="auto">
  <name>Task 3: Create filter and refresh components with data hooks</name>
  <files>admin-web/src/components/dashboard/year-filter.tsx, admin-web/src/components/dashboard/refresh-button.tsx, admin-web/src/app/dashboard-content.tsx</files>
  <read_first>
    - packages/shared-ui/src/components/ui/select.tsx (Select component)
    - packages/shared-ui/src/components/ui/button.tsx (Button component)
    - admin-web/src/lib/api/admin-client.ts (getStats)
  </read_first>
  <action>
    1. **admin-web/src/components/dashboard/year-filter.tsx**
       - Props: value: number, onChange: (year: number) => void
       - Uses Select component from shared-ui
       - Options: current year - 2 through current year + 1
       - Default: current year (new Date().getFullYear())
       - Select shows "选择年份" as placeholder
       - onChange triggers refetch of charts data

    2. **admin-web/src/components/dashboard/refresh-button.tsx**
       - Props: onRefresh: () => void, isLoading: boolean
       - Uses Button with variant="outline", size="sm"
       - Icon: RefreshCw from lucide-react
       - Label: "刷新数据"
       - When isLoading: icon spins (animate-spin class), button disabled
       - Uses tooltip or aria-label for accessibility

    3. **admin-web/src/app/dashboard-content.tsx** (UPDATE - add data hooks)
       - Add TanStack Query useQuery for stats: adminApiEndpoints.getStats()
       - Add selectedYear state (default: current year)
       - Add useQuery for income/occupancy (initially use mock data arrays)
       - YearFilter onChange updates selectedYear and refetches charts
       - RefreshButton onRefresh calls queryClient.invalidateQueries for all dashboard queries
       - Mock data for income: array of 12 IncomeReport objects (one per month)
       - Mock data for occupancy: array of 12 OccupancyReport objects (one per month)
       - Real data integration note: income/occupancy will need backend aggregate endpoint (see UI-SPEC API Notes)

    Mock data format for IncomeReport (per month):
    ```typescript
    { period: '2024-01', total_rent: 50000, total_water: 2000, total_electricity: 3000, total_other: 1000, total_amount: 56000, collected_amount: 48000, collection_rate: 85.7 }
    ```

    Mock data format for OccupancyReport (per month):
    ```typescript
    { period: '2024-01', total_rooms: 100, occupied_rooms: 85, vacant_rooms: 15, occupancy_rate: 85.0 }
    ```
  </action>
  <verify>
    <automated>grep -l "YearFilter\|RefreshButton" admin-web/src/components/dashboard/year-filter.tsx admin-web/src/components/dashboard/refresh-button.tsx admin-web/src/app/dashboard-content.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - admin-web/src/components/dashboard/year-filter.tsx has year options from (currentYear-2) to (currentYear+1)
    - admin-web/src/components/dashboard/year-filter.tsx has "选择年份" placeholder
    - admin-web/src/components/dashboard/refresh-button.tsx has RefreshCw icon
    - admin-web/src/components/dashboard/refresh-button.tsx shows animate-spin when isLoading
    - dashboard-content.tsx has useState for selectedYear
    - dashboard-content.tsx has queryClient.invalidateQueries on refresh
    - dashboard-content.tsx has mock data arrays for income and occupancy
  </acceptance_criteria>
  <done>YearFilter, RefreshButton, and data hooks created with mock data for charts</done>
</task>

</tasks>

<verification>
- All dashboard components exist and export correctly
- Page renders with 6 stat cards in responsive grid
- Charts render with mock data
- Year filter and refresh button functional
- No TypeScript errors (run pnpm type-check in admin-web)
</verification>

<success_criteria>
- admin-web/src/app/page.tsx shows "平台概览" title
- 6 stat cards display: 公寓数, 房间数, 入住率, 本月收入, 待缴账单, 逾期账单
- Income chart shows ComposedChart with bars + line
- Occupancy chart shows AreaChart with 80% reference line
- Year filter allows year selection
- Refresh button triggers data refetch
- Page builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-运营数据看板/08-01-SUMMARY.md`
</output>
