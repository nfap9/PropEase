---
phase: 01-统一设置页面布局
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tenant-web/src/components/ui/breadcrumb.tsx
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03

must_haves:
  truths:
    - "All settings sub-pages display breadcrumb navigation"
    - "Breadcrumb shows full hierarchy path (设置 > [当前页面])"
    - "Breadcrumb link on '设置' navigates to /settings"
  artifacts:
    - path: "tenant-web/src/components/ui/breadcrumb.tsx"
      provides: "shadcn/ui Breadcrumb components"
      min_lines: 50
    - path: "tenant-web/src/components/layout/settings-layout.tsx"
      provides: "Settings layout with breadcrumb navigation"
      exports: ["SettingsLayout", "SettingsBreadcrumb"]
    - path: "tenant-web/src/app/settings/layout.tsx"
      provides: "Next.js route group layout for /settings/*"
  key_links:
    - from: "tenant-web/src/app/settings/layout.tsx"
      to: "tenant-web/src/components/layout/settings-layout.tsx"
      via: "import and wrap with SettingsLayout"
    - from: "SettingsBreadcrumb"
      to: "/settings"
      via: "Link to home"
---

<objective>
创建 settings 统一布局，为所有子页面提供一致的返回导航体验和面包屑。

**Phase Goal:** 创建 settings 统一布局，提供一致的导航体验

**Purpose:** 统一的布局确保所有设置子页面有一致的导航结构，用户可以随时返回设置首页

**Output:**
- shadcn/ui Breadcrumb 组件
- SettingsLayout 组件（包含面包屑导航）
- settings/layout.tsx
</objective>

<execution_context>
@/Users/shen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@tenant-web/src/app/settings/page.tsx
@tenant-web/src/app/settings/team/page.tsx
@tenant-web/src/app/settings/permissions/page.tsx
@tenant-web/src/app/settings/notifications/page.tsx
@tenant-web/src/app/settings/subscription/page.tsx
@tenant-web/src/components/layout/main-layout.tsx
@tenant-web/src/components/layout/nav-config.ts

# User Decisions (locked)
- 返回按钮使用「文字+图标」形式，如「← 返回设置」
- 启用面包屑导航，显示完整层级路径：「设置 > 团队设置」
- 面包屑可点击，「设置」部分直接返回首页
- 移动端：面包屑自动折叠为「返回」
- 保持现有 URL 结构不变

# Page Titles for Breadcrumb (extracted from existing pages)
| Route | Page Title |
|-------|------------|
| /settings | 设置 |
| /settings/team | 团队设置 |
| /settings/permissions | 权限管理 |
| /settings/notifications | 租客消息触达 |
| /settings/subscription | 我的订阅 |
| /settings/subscription/purchase | 服务购买 |
| /settings/subscription/pay | 微信扫码支付 |
| /settings/subscription/result | 支付结果 |
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From tenant-web/src/components/layout/main-layout.tsx:
```typescript
export function MainLayout({ children }: { children: React.ReactNode }) {
  // Wraps with sidebar, header, and main content area
}
```

From tenant-web/src/app/settings/page.tsx:
```typescript
// Card grid structure with SettingsIcon heading
// Each card: icon + title + description
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn/ui Breadcrumb component</name>
  <files>tenant-web/src/components/ui/breadcrumb.tsx</files>
  <read_first>
    - tenant-web/components.json (shadcn config)
    - tenant-web/src/components/ui/button.tsx (style reference)
    - tenant-web/src/components/ui/separator.tsx (for style consistency)
  </read_first>
  <action>
    Run the following command to add the breadcrumb component:

    ```bash
    cd /Users/shen/workspace/apartment-ultra/tenant-web && npx shadcn@latest add breadcrumb -y --overwrite
    ```

    This will create `tenant-web/src/components/ui/breadcrumb.tsx` with the following exported components:
    - Breadcrumb
    - BreadcrumbEllipsis
    - BreadcrumbItem
    - BreadcrumbLink
    - BreadcrumbList
    - BreadcrumbPage
    - BreadcrumbSeparator

    The component should use Tailwind classes matching the project's style (based on shadcn new-york style).
  </action>
  <verify>
    <automated>test -f /Users/shen/workspace/apartment-ultra/tenant-web/src/components/ui/breadcrumb.tsx && grep -l "export.*Breadcrumb" /Users/shen/workspace/apartment-ultra/tenant-web/src/components/ui/breadcrumb.tsx</automated>
  </verify>
  <done>Breadcrumb component created at tenant-web/src/components/ui/breadcrumb.tsx with all required exports</done>
  <acceptance_criteria>
    - File exists: tenant-web/src/components/ui/breadcrumb.tsx
    - Exports: Breadcrumb, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator
    - Uses Tailwind classes consistent with project styling
    - No TypeScript errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create SettingsLayout component with breadcrumb navigation</name>
  <files>
    - tenant-web/src/components/layout/settings-layout.tsx
    - tenant-web/src/app/settings/layout.tsx
  </files>
  <read_first>
    - tenant-web/src/components/layout/main-layout.tsx (existing layout pattern)
    - tenant-web/src/components/ui/breadcrumb.tsx (just created)
    - tenant-web/src/components/ui/button.tsx (for back button style)
    - tenant-web/src/app/settings/page.tsx (for page title reference)
  </read_first>
  <action>
    Create `tenant-web/src/components/layout/settings-layout.tsx` with the following implementation:

    ```typescript
    'use client';

    import Link from 'next/link';
    import { usePathname } from 'next/navigation';
    import { MainLayout } from '@/components/layout/main-layout';
    import {
      Breadcrumb,
      BreadcrumbItem,
      BreadcrumbLink,
      BreadcrumbList,
      BreadcrumbPage,
      BreadcrumbSeparator,
    } from '@/components/ui/breadcrumb';
    import { Button } from '@apartment-ultra/shared-ui/components/ui';
    import { ChevronRight, ArrowLeft } from 'lucide-react';

    // Page title mapping based on route
    const PAGE_TITLES: Record<string, string> = {
      '/settings': '设置',
      '/settings/team': '团队设置',
      '/settings/permissions': '权限管理',
      '/settings/notifications': '租客消息触达',
      '/settings/subscription': '我的订阅',
      '/settings/subscription/purchase': '服务购买',
      '/settings/subscription/pay': '微信扫码支付',
      '/settings/subscription/result': '支付结果',
    };

    function getPageTitle(pathname: string): string {
      // Try exact match first
      if (PAGE_TITLES[pathname]) {
        return PAGE_TITLES[pathname];
      }
      // Try parent path for nested routes
      const parentPath = pathname.split('/').slice(0, -1).join('/');
      if (PAGE_TITLES[parentPath]) {
        return PAGE_TITLES[parentPath];
      }
      // Fallback: extract last segment
      const segments = pathname.split('/');
      return segments[segments.length - 1] || '设置';
    }

    function isSettingsHome(pathname: string): boolean {
      return pathname === '/settings';
    }

    export function SettingsBreadcrumb() {
      const pathname = usePathname();
      const pageTitle = getPageTitle(pathname);
      const isHome = isSettingsHome(pathname);

      if (isHome) {
        return null; // No breadcrumb on settings home page
      }

      return (
        <div className="flex items-center gap-2 text-sm">
          <Breadcrumb>
            <BreadcrumbList>
              <BreadcrumbItem>
                <BreadcrumbLink asChild>
                  <Link href="/settings">设置</Link>
                </BreadcrumbLink>
              </BreadcrumbItem>
              <BreadcrumbSeparator>
                <ChevronRight className="h-4 w-4" />
              </BreadcrumbSeparator>
              <BreadcrumbItem>
                <BreadcrumbPage>{pageTitle}</BreadcrumbPage>
              </BreadcrumbItem>
            </BreadcrumbList>
          </Breadcrumb>
        </div>
      );
    }

    export function SettingsLayout({ children }: { children: React.ReactNode }) {
      const pathname = usePathname();
      const isHome = isSettingsHome(pathname);

      return (
        <MainLayout>
          <div className="space-y-4">
            {/* Back button - only show on non-home pages */}
            {!isHome && (
              <div className="flex items-center gap-2">
                <Button variant="ghost" size="sm" asChild>
                  <Link href="/settings">
                    <ArrowLeft className="mr-1 h-4 w-4" />
                    返回设置
                  </Link>
                </Button>
              </div>
            )}
            {/* Breadcrumb navigation */}
            <SettingsBreadcrumb />
            {/* Page content */}
            {children}
          </div>
        </MainLayout>
      );
    }
    ```

    Then create `tenant-web/src/app/settings/layout.tsx`:

    ```typescript
    import { SettingsLayout } from '@/components/layout/settings-layout';

    export default function SettingsLayoutWrapper({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return <SettingsLayout>{children}</SettingsLayout>;
    }
    ```
  </action>
  <verify>
    <automated>test -f /Users/shen/workspace/apartment-ultra/tenant-web/src/components/layout/settings-layout.tsx && test -f /Users/shen/workspace/apartment-ultra/tenant-web/src/app/settings/layout.tsx && grep -l "export.*SettingsLayout" /Users/shen/workspace/apartment-ultra/tenant-web/src/components/layout/settings-layout.tsx</automated>
  </verify>
  <done>SettingsLayout component created with breadcrumb navigation, settings/layout.tsx wraps all sub-pages</done>
  <acceptance_criteria>
    - File exists: tenant-web/src/components/layout/settings-layout.tsx
    - File exists: tenant-web/src/app/settings/layout.tsx
    - Exports: SettingsLayout, SettingsBreadcrumb from settings-layout.tsx
    - Breadcrumb displays for all /settings/* routes except /settings itself
    - Back button displays "← 返回设置" on all sub-pages
    - Breadcrumb links: "设置" links to /settings
    - No TypeScript errors
  </acceptance_criteria>
</task>

</tasks>

<verification>
# Overall Phase Verification

1. Run type check:
   ```bash
   cd /Users/shen/workspace/apartment-ultra && pnpm --filter apartment-ultra-tenant type-check
   ```

2. Verify breadcrumb component:
   ```bash
   grep -c "BreadcrumbItem\|BreadcrumbLink" /Users/shen/workspace/apartment-ultra/tenant-web/src/components/ui/breadcrumb.tsx
   ```

3. Verify settings layout:
   ```bash
   grep -c "SettingsLayout\|SettingsBreadcrumb" /Users/shen/workspace/apartment-ultra/tenant-web/src/components/layout/settings-layout.tsx
   ```
</verification>

<success_criteria>
1. All settings sub-pages (/settings/team, /settings/permissions, /settings/notifications, /settings/subscription/*) display breadcrumb navigation
2. Breadcrumb shows full hierarchy path: "设置 > [当前页面名称]"
3. "设置" in breadcrumb is clickable and navigates to /settings
4. Sub-pages show "← 返回设置" button
5. Settings homepage (/settings) does NOT show breadcrumb or back button
6. All pages pass TypeScript type checking
</success_criteria>

<output>
After completion, create `.planning/phases/01-统一设置页面布局/01-01-SUMMARY.md`
</output>
