# 公寓管理系统 (Apartment Ultra) 实现计划

## Context

为二房东开发一套公寓管理系统，支持管理多个公寓、房间、租客和租约，实现水电记录、自动记账、账单生成和经营分析等功能。系统需要支持多用户协作管理同一组数据。

**技术栈:**
- 前端: Next.js 14 (App Router) + shadcn/ui + Tailwind CSS + TypeScript
- 后端: FastAPI (Python)
- 数据库: PostgreSQL
- 账单导出: PDF + Excel

---

## 项目结构

```
apartment-ultra/
├── frontend/                    # Next.js 14 前端
│   ├── src/
│   │   ├── app/                 # App Router 页面
│   │   ├── components/          # 组件
│   │   │   ├── ui/              # shadcn/ui
│   │   │   ├── layout/          # 布局组件
│   │   │   ├── forms/           # 表单组件
│   │   │   └── charts/          # 图表组件
│   │   ├── lib/api/             # API 客户端
│   │   ├── lib/auth/            # 认证逻辑
│   │   ├── hooks/               # 自定义 Hooks
│   │   ├── types/               # TypeScript 类型
│   │   └── store/               # Zustand 状态
│   └── package.json
│
├── backend/                     # FastAPI 后端
│   ├── app/
│   │   ├── main.py              # 入口
│   │   ├── config.py            # 配置
│   │   ├── database.py          # 数据库连接
│   │   ├── dependencies.py      # 依赖注入(认证/权限)
│   │   ├── models/              # SQLAlchemy ORM
│   │   ├── schemas/             # Pydantic 模型
│   │   ├── routers/             # API 路由
│   │   ├── services/            # 业务逻辑
│   │   └── utils/               # 工具(PDF/Excel)
│   ├── alembic/                 # 数据库迁移
│   └── requirements.txt
│
└── docker-compose.yml           # 开发环境
```

---

## 数据库设计 (核心表)

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `organizations` | 组织/团队 | name, slug, plan, settings |
| `users` | 用户 | email, password_hash, full_name |
| `organization_members` | 成员关系 | organization_id, user_id, role |
| `apartments` | 公寓 | organization_id, name, address |
| `rooms` | 房间 | apartment_id, room_number, status, monthly_rent |
| `tenants` | 租客 | organization_id, name, phone, id_card |
| `leases` | 租约 | room_id, tenant_id, start_date, end_date, monthly_rent, water_rate, electricity_rate |
| `utility_readings` | 水电记录 | room_id, period_year, period_month, water_reading, electricity_reading |
| `bills` | 账单 | lease_id, bill_year, bill_month, total_amount, paid_amount, status |
| `payments` | 支付记录 | bill_id, amount, payment_method |

---

## 实施阶段

### Phase 1: 基础架构
**目标:** 搭建项目骨架和基础设施

**步骤:**
1. 初始化 Next.js 14 项目 (App Router, TypeScript)
2. 配置 shadcn/ui 和 Tailwind CSS
3. 初始化 FastAPI 项目结构
4. 配置 PostgreSQL + SQLAlchemy 2.0
5. 设置 Alembic 数据库迁移
6. 创建基础 ORM 模型 (User, Organization)
7. 实现 JWT 认证系统 (注册/登录/Token刷新)
8. 创建 docker-compose.yml 开发环境

**关键文件:**
- `backend/app/models/base.py` - ORM 基类
- `backend/app/dependencies.py` - 认证中间件
- `backend/app/utils/security.py` - JWT/密码处理
- `frontend/src/lib/auth/context.tsx` - 认证上下文
- `frontend/src/lib/api/client.ts` - API 客户端

### Phase 2: 核心业务
**目标:** 实现公寓、房间、租客、租约管理

**步骤:**
1. 实现公寓 CRUD API 和前端页面
2. 实现房间 CRUD API 和前端页面
3. 实现租客 CRUD API 和前端页面
4. 实现租约管理:
   - 创建租约(检查房间状态、日期重叠)
   - 租约列表/详情/编辑
   - 终止租约流程
5. 创建数据表格组件 (排序/筛选/分页)

**关键文件:**
- `backend/app/services/lease_service.py` - 租约业务逻辑
- `frontend/src/components/forms/lease-form.tsx` - 租约表单
- `frontend/src/components/common/data-table.tsx` - 通用表格

### Phase 3: 财务功能
**目标:** 实现水电记录、账单生成、支付管理

**步骤:**
1. 实现水电记录 API (单个录入/批量录入)
2. 实现账单生成逻辑:
   - 根据水电读数计算费用
   - 批量生成月度账单
   - 手动创建账单
3. 实现支付记录功能
4. 实现 PDF 账单导出 (ReportLab)
5. 实现 Excel 账单导出 (openpyxl)

**关键文件:**
- `backend/app/services/bill_service.py` - 账单生成核心
- `backend/app/services/utility_service.py` - 水电记录
- `backend/app/utils/pdf_generator.py` - PDF 生成
- `backend/app/utils/excel_exporter.py` - Excel 导出

### Phase 4: 分析报表
**目标:** 实现仪表盘和经营分析

**步骤:**
1. 实现仪表盘 API (汇总数据)
2. 创建收入统计页面 (按月/年)
3. 创建入住率/空置率分析
4. 创建收缴率统计
5. 实现图表可视化 (Recharts)

**关键文件:**
- `backend/app/services/report_service.py` - 报表统计
- `frontend/src/components/charts/` - 图表组件

### Phase 5: 多用户协作
**目标:** 实现组织管理和权限控制

**步骤:**
1. 实现组织创建/切换
2. 实现成员邀请系统
3. 实现 RBAC 权限控制 (owner/admin/member/viewer)
4. 实现数据隔离 (organization_id)
5. 创建团队管理页面

---

## API 设计 (RESTful)

```
认证:
POST   /api/v1/auth/register
POST   /api/v1/auth/login
POST   /api/v1/auth/refresh
GET    /api/v1/auth/me

组织:
GET    /api/v1/organizations
POST   /api/v1/organizations
GET    /api/v1/organizations/{id}/members

公寓/房间/租客/租约:
GET    /api/v1/apartments
POST   /api/v1/apartments
GET    /api/v1/rooms
POST   /api/v1/rooms
... (标准 CRUD)

水电/账单:
POST   /api/v1/utilities
POST   /api/v1/utilities/batch
POST   /api/v1/bills/generate
GET    /api/v1/bills/{id}/pdf
GET    /api/v1/bills/{id}/excel
POST   /api/v1/bills/{id}/payments

报表:
GET    /api/v1/reports/overview
GET    /api/v1/reports/income
GET    /api/v1/reports/occupancy
```

---

## 前端页面路由

```
/login                 # 登录
/register              # 注册
/dashboard             # 仪表盘
/apartments            # 公寓列表
/apartments/[id]       # 公寓详情
/rooms                 # 房间列表
/tenants               # 租客列表
/leases                # 租约列表
/leases/new            # 创建租约
/utilities             # 水电录入
/bills                 # 账单列表
/bills/generate        # 生成账单
/reports               # 经营分析
/settings/team         # 团队管理
```

---

## 技术依赖

### 前端
- Next.js 14, TypeScript
- shadcn/ui, Tailwind CSS
- React Hook Form, Zod
- TanStack Query, Axios
- Recharts, date-fns
- Zustand (可选)

### 后端
- FastAPI
- SQLAlchemy 2.0, Alembic
- Pydantic v2
- python-jose (JWT)
- Passlib (密码)
- ReportLab (PDF)
- openpyxl (Excel)

---

## 验证方式

1. **Phase 1 验证:**
   - `docker-compose up` 启动服务
   - 访问 `http://localhost:3000` 显示登录页
   - 注册/登录功能正常
   - API 文档可访问 `http://localhost:8000/docs`

2. **Phase 2 验证:**
   - 创建公寓 → 添加房间 → 添加租客 → 创建租约
   - 房间状态自动更新

3. **Phase 3 验证:**
   - 录入水电 → 生成账单 → 记录支付
   - 导出 PDF/Excel 文件正常

4. **Phase 4 验证:**
   - 仪表盘显示统计数据
   - 图表正常渲染

5. **Phase 5 验证:**
   - 邀请新成员 → 切换角色 → 验证权限
